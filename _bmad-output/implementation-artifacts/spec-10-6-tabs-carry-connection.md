---
title: 'Tabs carry their connectionId — persist it in the WorkspaceSnapshot and survive the connection being removed'
type: 'feature'
created: '2026-07-21'
status: 'done'
baseline_revision: '4ad9393'
final_revision: '161cfe8'
review_loop_iteration: 0
followup_review_recommended: false
depends_on:
  - '10-4-core-resolve-by-connection-id'
  - '10-5-multi-root-schema-tree'
context:
  - '{project-root}/_bmad-output/planning-artifacts/epics.md'  # Epic 10 / Story 10.6
  - '{project-root}/_bmad-output/planning-artifacts/epic-10-multi-connection-tree.mockup.html'
  - '{project-root}/src/ui/workspace/workspace-state.ts'
warnings:
  - oversized
---

<intent-contract>

## Intent

**Problem:** The workspace is becoming multi-connection (10.4 makes every read path resolve by `connectionId`; 10.5 turns the schema sidebar into a multi-root tree, one root per saved connection). But `TableRef` (`src/ui/workspace/workspace-state.ts:49`) is still `{ schema, name }` only — it has no idea WHICH connection a bound table came from. And even if it did, that fact evaporates on restart: `toWorkspaceSnapshot` deliberately drops `table` from every persisted tab (Story 3.2's Block-If — "the open-table binding does not survive an app restart"), and `WorkspaceSnapshotTab` (`src/shared/contract.ts:736`) carries only `{ id, kind, title }`. So today a restored table tab is always unbound, and there is no way — even in principle — to know which of N connections it was ever pointed at. Once the tree is multi-root, "reopen the same tab against the same database" requires that fact to survive a relaunch, AND the referenced connection may no longer exist by then (it was removed in the meantime) — which today would have no representation at all, let alone a non-crashing one.

**Approach:** Give `connectionId` two homes: (1) `TableRef` gains `connectionId: string | null` (`null` = the boot/default target, mirroring the existing `null`-means-default convention already used by `ExecuteRequest.connectionId` in `contract.ts`) — this is the LIVE binding, threaded from the multi-root tree's activation callback (10.5) through `bindTableToActiveTab` into `table.rows`/`execute` RPC calls (10.4). (2) `WorkspaceTab` (the reducer's in-memory tab, `workspace-state.ts:55`) gains its OWN top-level `connectionId?: string | null` — decoupled from `table`, because `table` itself still does not survive restore (that Story-3.2 decision is NOT reversed by this story: schema+name binding stays session-only). This top-level field is the one thing that DOES persist: `WorkspaceSnapshotTab` gains an additive, optional `connectionId?: string | null` (no `WORKSPACE_SNAPSHOT_VERSION` bump — same additive posture as `erdLayouts`/`lastProvider`), `toWorkspaceSnapshot` writes it whenever a table tab has ever been bound, and `restoreWorkspace` carries it onto the restored tab (still unbound — `table` stays `undefined`, exactly like today) so the tab "remembers" its connection even though it forgot its table. A render-layer check (App/Workspace — NOT the pure `workspace-state.ts`, which cannot itself call `connections.list`) then compares each tab's `connectionId` against the live saved-connection set and renders a "conexión no disponible" state, with a reassign affordance, for that ONE tab — never a crash, never a dropped tab, never a tanked sibling.

## Boundaries & Constraints

**Always:**
- Add `connectionId: string | null` to `TableRef` (`workspace-state.ts:49`). `null` means the boot/default connection — the SAME convention `ExecuteRequest.connectionId?: string | null` already uses (`contract.ts:876-877`), so there is exactly one "default target" encoding across the codebase, not a second one invented here.
- Add a top-level `connectionId?: string | null` to `WorkspaceTab` (`workspace-state.ts:55`), independent of `table?: TableRef`. Populate it whenever a table tab is bound (`bindTableToActiveTab`, mirroring `ref.connectionId`) so the live and persisted values never disagree while the tab is bound.
- Add `connectionId?: string | null` to `WorkspaceSnapshotTab` (`contract.ts:736`) as an ADDITIVE optional field — `WORKSPACE_SNAPSHOT_VERSION` stays `1`. `toWorkspaceSnapshot` writes it only when present (mirrors the existing `erdLayouts`/`lastProvider` "no gratuitous field" posture — an untouched/no-connection workspace still serializes byte-identically to today).
- `restoreWorkspace` carries a tab's persisted `connectionId` onto the restored `WorkspaceTab` verbatim (defaulting an absent field to `undefined`, never throwing) — it stays PURE and does not itself decide reachability (it has no RPC access; `connections.list` is Core-side).
- An old snapshot with no `connectionId` on a tab restores as `undefined` — treated identically to `null` (the boot/default target) at every read site, so a pre-10.6 workspace file loads with no behavior change and no forced "conexión no disponible" for tabs that simply predate this story.
- The missing-connection check happens at the App/Workspace render layer, AFTER both `workspace.load` and `connections.list` resolve, by comparing each tab's `connectionId` (when non-null/non-undefined) against the live `ConnectionSummary.id` set. A tab whose id is absent renders a "conexión no disponible" state (Spanish copy, per the mockup: `conexión no disponible (fue eliminada)`) with a "Reasignar conexión…" affordance, IN PLACE of the normal table body — it never throws, never removes the tab, and never touches any sibling tab's render.
- Core's save-boundary validators (`checkTabs` in `workspace-registry.ts:85`, `isSnapshotTab` in `workspace-store.ts:140`) accept the new optional field: at SAVE (the trusted, strict boundary) `connectionId` must be `undefined`, `null`, or a non-empty string, else `bad_request` naming the field — mirroring how `id`/`kind`/`title` are already strictly checked there. At LOAD (the total, never-throw boundary) a malformed `connectionId` must NOT nuke the whole snapshot — sanitize/drop just that field, mirroring `lastProvider`'s documented "a single bad hint must never discard every tab" posture (`workspace-store.ts:175-179`), rather than degrading the entire file to `null`.
- The migration must never silently drop a tab: every existing tab in an old snapshot restores exactly as it does today, plus the new (absent) field — `restoreWorkspace`'s id-dedupe/settings-singleton defenses are unchanged.
- Thread `connectionId` through wherever a table tab is opened from the tree (10.5's multi-root activation callback) and wherever a bound table's rows are fetched (`table.rows` in `TabContent.tsx`'s `TableTabView`) — this story's job is to CARRY the id end-to-end once 10.4/10.5 hand it in and out; it does not itself build the multi-root tree or the connectionId-aware RPC surface.

**Block If:**
- If a restored tab's `connectionId` cannot be compared against the live connection set without giving the pure `workspace-state.ts` module an RPC dependency (breaking its stated dependency-free/DOM-free contract) — HALT `blocked`, condition `connection-availability check cannot be kept out of the pure reducer module`. (Expected resolution per this draft: keep the check at the App/Workspace layer, as scoped above — flag if step-02 finds a reason that is not viable.)
- If 10.5's multi-root tree activation callback cannot supply a `connectionId` alongside the activated `SchemaTableInfo` without a breaking signature change agreed upon in 10.5's own spec — HALT `blocked`, condition `10.5 activation callback does not carry connectionId`, and coordinate the exact callback shape with that story rather than guessing it here.

**Never:**
- NEVER crash the workspace restore because one tab's connection is missing — `restoreWorkspace` and the render-layer check are both total; a missing connection is a per-tab UI state, not an exception.
- NEVER tank sibling tabs — a tab with a missing connection renders its own "conexión no disponible" body; every other tab (any kind, any connection, or no connection) renders normally, completely unaffected.
- NEVER silently drop a tab or its title/kind because its connection vanished — the tab stays in the strip, closable and reassignable, exactly like any other tab.
- NEVER let a raw connection url/credential leak into the persisted snapshot or the "conexión no disponible" UI — only the opaque `connectionId` crosses any boundary (AR-12), matching 10.4's hard invariant.
- NEVER bump `WORKSPACE_SNAPSHOT_VERSION` for this additive field.
- NEVER reverse Story 3.2's decision that the live table binding (`schema`/`name`) does not persist — this story persists ONLY `connectionId`, not the rest of `TableRef`. (Open question for step-02, see below.)

**Open question for step-02:** Epic 10's own Story 10.6 AC text says "a table/query tab" carries a `connectionId`, but the authoritative solution point and the only existing per-tab connection-scoped ref type (`TableRef`) cover table tabs only — query tabs have no equivalent bound-ref today. This draft scopes `WorkspaceTab.connectionId`/`WorkspaceSnapshotTab.connectionId` generically (any tab kind CAN carry it), but only wires table tabs to actually populate it. Whether query tabs should also carry/persist a `connectionId` in this story or a follow-up is left for step-02 to confirm against 10.4/10.5's actual query-tab RPC shape.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|----------------|------------------------------|-----------------|
| Open a table tab from a connection root | Activate a table under connection `conn-2` in the multi-root tree (10.5) | `bindTableToActiveTab` sets `table: { connectionId: "conn-2", schema, name }` AND the tab's top-level `connectionId: "conn-2"`; `table.rows` is called with `connectionId: "conn-2"` | No error |
| Open a table tab from the boot/default target | Activate a table with no explicit connection (ephemeral, or the boot manager) | `connectionId: null` on both `table` and the tab; RPCs omit/pass `null`, resolving to the boot manager (10.4) | No error |
| Save + restore against the SAME still-existing connection | `workspace.save` while a table tab is bound to `conn-2`, then relaunch | Snapshot carries `connectionId: "conn-2"` on that tab; on restore the tab reopens with `connectionId: "conn-2"` (table binding itself stays unbound, same as today); no "conexión no disponible" (id found in `connections.list`) | No error |
| Restore a tab whose connection was deleted | Snapshot tab has `connectionId: "conn-2"`; `connections.list` no longer includes `conn-2` | Tab renders a "conexión no disponible (fue eliminada)" state with a "Reasignar conexión…" affordance, in place of the table body; the tab itself, its title, and its position in the strip are all preserved | No crash; no thrown error; workspace restore completes normally |
| Old snapshot, tab has no `connectionId` field | Pre-10.6 snapshot, `tabs[].connectionId` absent | Restores as `undefined`, treated as the boot/default target everywhere (never flagged unavailable) — behavior identical to today | No error, no forced reassign prompt |
| One tab's connection is missing among several open tabs | Tabs bound to `conn-1` (exists), `conn-2` (deleted), and a query tab with no connection | Only the `conn-2` tab shows "conexión no disponible"; the `conn-1` table tab and the query tab render and behave exactly as normal | Isolated — no sibling impact |
| Core save-boundary validation | `workspace.save` with a tab `connectionId` that is a non-empty string, `null`, or absent | Accepted — `checkTabs`/`isSnapshotTab` pass; snapshot persists | — |
| Core save-boundary validation, malformed value | `workspace.save` with a tab `connectionId` that is e.g. a number or empty string | `bad_request` naming the offending field; nothing written | Rejected at the trusted write boundary |
| Load-time malformed value (hand-edited file) | On-disk snapshot has a tab with `connectionId: 42` (wrong type) | Load sanitizes/drops just that field (tab restores with `connectionId: undefined`) rather than degrading the WHOLE snapshot to `null` | Tolerated, isolated to the one field |
| Reassign affordance | User clicks "Reasignar conexión…" on an unavailable tab | The tab's `connectionId` is updated to a newly chosen live connection; the "conexión no disponible" state clears and the tab returns to its normal (still-unbound, per 3.2) table-selection state | No error |

</intent-contract>

## Code Map

> Line references verified against `HEAD` = `4ad9393` (Story 10.5 landed). **The intent contract's line numbers predate 10.4/10.5** — reconciled here: `WorkspaceSnapshotTab` is at `contract.ts:809-813` (not `:736`), `ExecuteRequest.connectionId` at `:948-950` (not `:876`), and `TableRef.connectionId` **already exists** (10.5 added it as `?: string | null`, session-only) so this story does not add it — it makes it *persistable* and reconciles its doc comment.

- `src/ui/workspace/workspace-state.ts` (417 lines) — the pure, DOM-free, dependency-free tab model ("All Tab lifecycle logic … pure, total functions so it is unit-testable with no DOM and no React harness (FR-23)"). `:49-63` `TableRef` — already `{schema, name, connectionId?: string | null}`; its doc comment currently ends "PERSISTING and RESTORING it is Story 10.6" (this story retires that sentence). `:65-76` `WorkspaceTab` = `{id, kind, title, table?}` — gains the top-level `connectionId`. `:231-247` `bindTableToActiveTab(state, ref)` — the ONLY writer of `table:`; reuses the active tab iff `active !== null && active.kind === "table"`, else appends a new `table` tab; `title = ref.name` verbatim (AR-19). `:298-326` `restoreWorkspace(snapshot)` — "pure and total", rebuilds each tab as `{id, kind, title}` (dropping every other field), then the id-dedupe (DW-26, keep FIRST per id) and settings-singleton (Story 8.6) passes, then `maxId`/`nextId`/`activeTabId` reconciliation. `:382-417` `toWorkspaceSnapshot(state, panelSizes, erdLayouts?, lastProvider?)` — filters out `create-table` (tsc-enforced via the type-guard filter), maps tabs to `{id, kind, title}`, then attaches `erdLayouts` only when the pruned map is non-empty and `lastProvider` only when `lastProvider != null` — the exact "no gratuitous field" idiom this story mirrors. Also exports `openTab`/`closeTab`/`activateTab`/`openOrFocusSettings`/`openOrFocusCreateTable`/`sanitizePanelSizes`/`restoreErdLayouts`/`restoreLastProvider` (`:368`, the UI-side sanitize precedent: unknown → `null`).
- `src/shared/contract.ts` — `WORKSPACE_SNAPSHOT_VERSION = 1 as const` (`:806`, stays `1`); `WorkspaceSnapshotTab` (`:809-813`) `{id, kind: WorkspaceTabKind, title}` — the additive field lands here; `WorkspaceSnapshot` (`:846-854`) with its doc block (`:829-845`) spelling out the additive posture verbatim for `erdLayouts` ("optional so a pre-4.2 v1 snapshot … still loads cleanly … hence `WORKSPACE_SNAPSHOT_VERSION` stays `1` (no version bump, which would discard existing persisted workspaces)") and `lastProvider`. `ConnectionSummary` (`:394-415`) `{id, name, host, engine, schema?}` — the live-set element and the reassign picker's data source; `ListConnectionsResult` (`:475`). `ConnectRequest.connectionId` (`:333-336`) / `TableRowsRequest.connectionId` (`:359-366`) / `ChatStreamRequest.connectionId` (`:590-596`) — the three sites that spell out "absent/`null` ⇒ the boot connection", the wording the new field's doc mirrors. `ExecuteRequest` (`:948-950`).
- `src/core/workspace-registry.ts` — `badRequest(field, reason)` (`:67-69`) → `{ok:false, code:"bad_request", message: reason, detail: "field=" + field}` (tests assert `detail` contains the field). `checkTabs` (`:85-119`) — per-tab `id`/`kind`/`title` checks plus the DW-26 uniqueness `Set`; **returns `value as WorkspaceSnapshotTab[]` — the RAW objects, verbatim**, so a new tab field passes through to the store with no rebuild (and, today, with no validation). `validateSnapshotParams` (`:176-230`) — calls `checkTabs` at `:194-195` (always `badRequest("tabs", …)`, the sub-reason rides in `message`), then `activeTabId` (DW-25) / `nextId` / `checkErdLayouts`, and returns the snapshot with `tabs: tabs.value` verbatim and `erdLayouts` spread only when present. Called from `save(params)` (`:271`) BEFORE any store write.
- `src/core/workspace-store.ts` — `isSnapshotTab` (`:139-149`), a boolean shape guard on `{id finite number, kind via isTabKind, title string}`; `isWorkspaceSnapshot` (`:157-181`) calls `v.tabs.every(isSnapshotTab)` at `:167`, so **one bad tab degrades the WHOLE snapshot to `null`** (`load()` at `:228`). The contrasting field-drop precedent is the `lastProvider` comment at `:175-179`: "a FIELD-DROP field, deliberately NOT gated here … an unrecognized value … must never discard the whole workspace, so the sanitizing lives UI-side in `restoreLastProvider` … Rejecting the snapshot over one bad hint would nuke every tab/panel/ERD layout, which is exactly what we avoid." That is the posture the new field adopts.
- `src/ui/App.tsx` (705 lines) — `WorkspaceAction` union (`:80-87`) + `workspaceReducer` (`:89-106`); `useReducer(workspaceReducer, undefined, emptyWorkspace)` (`:238`); `workspace.load` mount effect (`:477-518`) dispatching `{type:"restore", snapshot}` and flipping `savingEnabled`/`workspaceReady` (`:512-513`); `onActivateTable` (`:669-677`) already dispatches `{type:"bindTable", ref:{schema, name, connectionId}}`; `onSchemaLoaded` (`:339-350`) boot-root-only (`:343`); `primaryKeys` (`:428-441`) and `indexes` (`:446-455`) with their non-null-`connectionId` bail (`:438`, `:452`) — **unchanged by this story**. App calls `connections.list` nowhere.
- `src/ui/workspace/Workspace.tsx` (440 lines) — the shell; makes NO rpc call today. `:320` `const [registryRevision, setRegistryRevision] = useState(0)` with the rationale comment at `:314-319` ("the nearest common ancestor of the tree and the Settings tab"); `:344-350` the `<SchemaTree>` call site (receives `registryRevision`); `:388-410` the `<TabContent>` call site; `:399` `onRegistryChanged={() => setRegistryRevision((n) => n + 1)}`. This is where the live `ConnectionSummary[]` gets fetched and threaded down.
- `src/ui/workspace/TabContent.tsx` (674 lines) — `TabContent` props (`:459-521`) and the sequential per-kind dispatch: `table` (`:548-563`) → `tab.table !== undefined ? <TableTabView key={`${tab.table.connectionId ?? ""}::${schema}.${name}`} …/> : <SelectTablePrompt/>`; then `query` (`:565-578`), `chat` (`:580-593`), `erd` (`:595-608`), `report` (`:610-621`), `settings` (`:623-637`), `create-table` (`:639-655`), fallback (`:657-673`). **`TabContent` only ever receives the ACTIVE tab**, so sibling isolation is structural. `SelectTablePrompt` (`:471-481`) is the markup/idiom the new unavailable body mirrors (`flex h-full flex-col items-center justify-center gap-2 p-8 text-center lowercase text-[var(--muted-foreground)]`, `font-mono`/`12px`, an `11px` sub-paragraph). `TableTabView` (`:86-95` props) with `connectionScope` (`:140-141`), the `table.rows` effect (`:150-187`), `runOp`'s `execute` (`:194-218`) — **all unchanged by this story** (10.5 already threaded them).
- `src/ui/schema/SchemaTree.tsx` — `onActivate: (table, connectionId) => void` (`:428`) and `onSchemaLoaded?` (`:438`) already carry the id (10.5); `activateTable` (`:736-749`) calls `onActivate(table, connectionId)` at `:742`. **No change needed** — the Block-If about the activation callback is already satisfied.
- `src/ui/settings/SettingsPanel.tsx:310,330,345` — the three `onRegistryChanged()` fire sites (successful add/edit/remove) that make the new `connections.list` refetch land without a restart.
- `src/ui/styles/globals.css` — semantic tokens: `text-err`/`bg-err`/`bg-err-soft`/`border-err-line`, `text-muted-foreground`/`bg-muted`, `border-border`, `--radius`, `--font-mono`. Use these, NOT raw `red-*` utilities (DW-54 posture; `TabContent.tsx:409-438`'s literal `red-700`/`amber-700` banners are the anti-example, out of scope to fix here).
- `_bmad-output/planning-artifacts/epic-10-multi-connection-tree.mockup.html:188-195,326-330` — the missing-connection state: copy `conexión no disponible (fue eliminada)` in `.inline-msg.err` (`11px`, `color: var(--err)`) plus `<button class="retry">Reasignar conexión…</button>` (`11px` mono, `1px solid var(--border)`, `var(--radius)`, `3px 10px`, hover `var(--muted)`).
- Test idiom — `bun:test` (`describe`/`test`/`expect`) everywhere. UI components: `mock.module("../rpc/client.ts", () => ({rpc: rpcMock}))` BEFORE a dynamic `await import(...)`, then `renderToStaticMarkup` over an EXPORTED presentational sub-component (effects never run; the repo has no jsdom) — `src/ui/schema/SchemaTree.test.tsx:26`, `src/ui/workspace/ChatTabView.test.tsx:38`, `src/ui/report/ReportTabView.test.tsx:21`. Existing suites to extend: `src/ui/workspace/workspace-state.test.ts` (63 tests), `src/core/workspace-registry.test.ts` (`describe("workspace-registry — bad_request on malformed save params")`, `:241`/`:251`/`:291`), `src/core/workspace-store.test.ts` (`describe("workspace-store — degrade to null on malformed disk content, never a throw")` `:266`, round-trip `:44`, additive-field precedent `:111`/`:122`). **No test file exists for `TabContent.tsx`, `Workspace.tsx`, or `App.tsx`.**

## Tasks & Acceptance

**Execution:**

- [x] `src/shared/contract.ts` -- add `readonly connectionId?: string | null` to `WorkspaceSnapshotTab` (`:809-813`) with a doc comment in the house style: ADDITIVE + optional (Story 10.6), `WORKSPACE_SNAPSHOT_VERSION` stays `1` (a pre-10.6 snapshot has none and loads cleanly), `absent`/`null` ⇒ the boot/default target — the same convention `ConnectRequest`/`TableRowsRequest`/`ChatStreamRequest.connectionId` already spell out — and it carries the OPAQUE id ONLY (never a url, user, password or engine string, AR-12). Extend the `WorkspaceSnapshot` doc block (`:829-845`) with one sentence placing this field alongside `erdLayouts`/`lastProvider`. -- Rationale: this is the wire+disk shape the whole story hangs on, and the additive-posture doc is what stops a future reader from "fixing" it with a version bump that would discard every persisted workspace.
- [x] `src/ui/workspace/workspace-state.ts` -- (a) `WorkspaceTab` gains `readonly connectionId?: string | null`, documented as INDEPENDENT of `table?`: it is the only connection fact that survives a restart, `undefined` and `null` both mean the boot/default target, and it exists precisely because `table` still does not persist (Story 3.2 stands). (b) Retire the "PERSISTING and RESTORING it is Story 10.6" sentence in `TableRef.connectionId`'s doc (`:52-61`) and point it at the tab-level mirror; keep the field OPTIONAL (`?: string | null`) — 10.5 already added it in exactly the shape the contract asks for, and `null`-or-absent ⇒ boot is the codebase-wide convention. (c) `bindTableToActiveTab` (`:231-247`) sets `connectionId: ref.connectionId ?? null` on the tab in BOTH branches (rebinding the active table tab, and appending a new one) so the live tab-level value can never drift from `ref.connectionId`. (d) `toWorkspaceSnapshot` (`:400`) maps each persisted tab as `{id, kind, title, ...(t.connectionId != null ? {connectionId: t.connectionId} : {})}` — `!= null` covers `null` AND `undefined`, mirroring the `lastProvider` line at `:417` verbatim. (e) `restoreWorkspace` (`:298-326`) carries the field onto the restored tab, SANITIZING as it goes: `typeof t.connectionId === "string" && t.connectionId.length > 0` → keep, anything else (absent, `null`, wrong type from a hand-edited file) → omit the key entirely; `table` still stays `undefined` and the dedupe/singleton passes are untouched. (f) NEW export `setTabConnection(state: WorkspaceState, tabId: number, connectionId: string | null): WorkspaceState` — sets that one tab's `connectionId` and CLEARS its `table` (the old binding pointed at a different database and must not be reused), returns the SAME state reference when no tab matches the id; total, no throw. (g) NEW export `isTabConnectionMissing(tab: WorkspaceTab, liveIds: ReadonlySet<string> | null): boolean` — `false` when `liveIds === null` (the live set is not known yet: never flag on an unresolved/failed fetch), `false` when `tab.connectionId == null` (boot/default), else `!liveIds.has(tab.connectionId)`. -- Rationale: (d)/(e) are the persistence bridge; (g) is how the availability decision stays in the pure, unit-testable module while the RPC that feeds it stays at the render layer — the contract's Block-If resolved exactly as its expected resolution predicted.
- [x] `src/core/workspace-registry.ts` -- in `checkTabs` (`:85-119`), after the `title` check and before the uniqueness `Set`, strictly validate a PRESENT `connectionId`: `tab.connectionId !== undefined` must be `null` or a non-empty string, else `{ok:false, reason: "each tab.connectionId must be null or a non-empty string"}` (surfaced by the existing `badRequest("tabs", reason)` at `:194-195`, so `detail` stays `field=tabs` and the specific field rides in `message`, exactly like every sibling check). Do NOT rebuild the tab objects — `checkTabs` already returns them verbatim, which is what carries the new field through `validateSnapshotParams` to the store. -- Rationale: this is the trusted write boundary; without the check a UI bug could persist `connectionId: 42` and every later load would have to cope with it.
- [x] `src/core/workspace-store.ts` -- leave `isSnapshotTab` (`:139-149`) UNGATED for `connectionId` and add a comment saying so in the same voice as the `lastProvider` note at `:175-179`: the field is a FIELD-DROP field, sanitized UI-side in `restoreWorkspace`, because `isWorkspaceSnapshot` is all-or-nothing over `tabs` (`:167`) so gating it here would let one hand-edited value nuke every tab, panel size and ERD layout — the opposite of the contract's LOAD rule. -- Rationale: the contract demands "sanitize/drop just that field, never the whole snapshot"; in THIS store that means not gating at all, because the only gate available is a total rejection.
- [x] `src/ui/workspace/Workspace.tsx` -- own the live connection set: `const [connections, setConnections] = useState<ReadonlyArray<ConnectionSummary> | null>(null)` plus an effect keyed on `[registryRevision]` that fires `rpc<ListConnectionsResult>("connections.list")` and, guarded by the usual `alive` flag, writes the summaries on `ok` and leaves the previous value untouched on `!ok` (a failed registry read must never flag a tab as unavailable). Pass `connections` and `onReassignConnection` down to `<TabContent>` (`:388-410`). `registryRevision` already bumps on every successful Settings add/edit/remove, so this refetch reconciles without a restart, exactly like the tree's. -- Rationale: `Workspace` is the nearest common ancestor that already owns registry-invalidation state (`:314-319`), and the check needs BOTH the tab (from `TabContent`) and the live id set; hoisting it to `App` would thread two more props through the shell for no gain.
- [x] `src/ui/workspace/TabContent.tsx` -- (a) new optional props `connections?: ReadonlyArray<ConnectionSummary> | null` and `onReassignConnection?: (tabId: number, connectionId: string) => void`, documented as Story 10.6. (b) In the `table` branch (`:548-563`), derive `liveIds` (a memo/`useMemo`-free local: `connections == null ? null : new Set(connections.map((c) => c.id))`) and, when `isTabConnectionMissing(tab, liveIds)`, render `<ConnectionUnavailable …/>` INSTEAD of both `TableTabView` and `SelectTablePrompt`; otherwise the existing ternary is untouched, `key` included. (c) NEW exported presentational `ConnectionUnavailable({tabTitle, connections, onReassign}: {…})` — mirrors `SelectTablePrompt`'s centered mono layout, renders the mockup copy VERBATIM: `conexión no disponible (fue eliminada)` in `text-err`, a muted `11px` line naming the tab (`tab "<title>"`), and a `Reasignar conexión…` button styled with `border border-border rounded-[var(--radius)] px-2.5 py-[3px] text-[11px] hover:bg-muted`. Clicking it reveals an inline list of the live connections, each a `<button>` labelled `name · host` (engine badge optional) that calls `onReassign(id)`; when the live list is EMPTY the button is `disabled` and an `11px` muted hint says to add one in Settings. Tokens only — no `red-*`/`amber-*` literals — and NEVER render anything but `name`/`host`/`engine` from a summary (AR-12). -- Rationale: `TabContent` only ever receives the active tab, so putting the swap here gives per-tab isolation for free; exporting the body is what makes it assertable under `renderToStaticMarkup`, which cannot reach state produced by effects.
- [x] `src/ui/App.tsx` -- add `{type: "setTabConnection"; id: number; connectionId: string | null}` to `WorkspaceAction` (`:80-87`) and the matching `workspaceReducer` case (`:89-106`) delegating to `setTabConnection`; pass `onReassignConnection={(id, connectionId) => dispatch({type:"setTabConnection", id, connectionId})}` into `<Workspace>`. The debounced `workspace.save` already fires on reducer state changes, so the reassignment persists with no extra wiring. `primaryKeys`/`indexes` and their non-null-`connectionId` bail stay EXACTLY as 10.5 left them. -- Rationale: the reducer is App-owned; this is the one new action the story needs, and routing it through the existing dispatch keeps the save path unchanged.
- [x] `src/ui/workspace/workspace-state.test.ts` -- extend (additive) with a `describe` per new behavior: `bindTableToActiveTab` mirrors `ref.connectionId` onto the tab (saved id, and `null` for the boot target) in BOTH the rebind and the new-tab branch; `toWorkspaceSnapshot` emits `connectionId` for a saved-connection tab, emits NOTHING for a boot-bound (`null`) or never-bound tab (assert the key is absent via `"connectionId" in tab`, so the byte-identical claim is provable) and still never emits `table`; a full round-trip (`bind → toWorkspaceSnapshot → restoreWorkspace`) preserves the id while leaving `table` undefined; `restoreWorkspace` on a pre-10.6 snapshot yields `connectionId: undefined` on every tab with the tab count, kinds, titles, ids, `activeTabId` and `nextId` all unchanged; `restoreWorkspace` sanitizes a hand-edited `connectionId` of `42`, `""`, `{}` and `null` to an absent key without throwing and without dropping the tab; `setTabConnection` sets the id, clears `table`, leaves every sibling tab reference-identical, and returns the same state reference for an unknown id; `isTabConnectionMissing` truth table (`null` live set → false; `undefined`/`null` tab id → false; present id → false; absent id → true). **One existing test must be REWRITTEN, not deleted:** `"toWorkspaceSnapshot emits no connectionId anywhere for a connection-scoped table tab"` (in `describe("TableRef.connectionId NON-persistence (Story 10.5 — session-only)")`) asserts precisely what this story reverses — restate it as "the TABLE ref (schema/name) still never persists, while the tab-level `connectionId` now does", keeping the `table`-absence assertion intact and updating the describe title + comment to say 10.6 landed it. -- Rationale: this is the only test in the repo whose premise the story supersedes; leaving it would fail the build, deleting it would silently drop the Story-3.2 guarantee it also encodes.
- [x] `src/core/workspace-registry.test.ts` + `src/core/workspace-store.test.ts` -- registry: `connectionId` of `null`, a non-empty string, and absent all SAVE fine (and the saved value round-trips out through `load`, proving the verbatim pass-through); `connectionId` of `42`, `""`, `{}` and `true` each return `ok:false` / `code:"bad_request"` / `detail` containing `tabs` with nothing written (assert via a follow-up `load`). Store: a snapshot carrying `connectionId` round-trips through save+reopen (mirroring the `erdLayouts` additive test at `:111`); a pre-10.6 snapshot with no `connectionId` still loads (mirroring `:122`); and a hand-written on-disk snapshot whose tab has `connectionId: 42` still loads NON-null with every other tab intact (the field-drop posture — contrast with the existing "unknown tab kind → null" test at `:266`, which must keep passing unchanged). -- Rationale: the save boundary is strict and the load boundary is tolerant *by design*; only a test pins that asymmetry down, and the "still loads non-null" case is the one the contract calls out explicitly.
- [x] `src/ui/workspace/TabContent.test.tsx` -- **NEW**, following `SchemaTree.test.tsx`'s idiom (`mock.module("../rpc/client.ts", …)` before a dynamic `await import`, then `renderToStaticMarkup`): assert `ConnectionUnavailable` renders the exact strings `conexión no disponible (fue eliminada)` and `Reasignar conexión…`, carries `text-err` and NO `red-`/`amber-` substring, names the tab title, lists one reassign button per live connection showing `name`/`host` and NEVER any url/user/password-shaped text, and renders the disabled button + Settings hint when the live list is empty. Also assert the mocked `rpc` was called ZERO times during that render. -- Rationale: the "conexión no disponible" body is this story's only new user-visible surface and there is no existing test file for `TabContent`; the zero-call assertion keeps the presentational component honest about being effect-free.
- [x] `_bmad-output/implementation-artifacts/deferred-work.md` -- add **DW-70** (the next free id — `DW-69` is already taken by the 10.5 follow-up-review entry; origin: this story, severity low, status open): query and chat tabs still execute against the boot target regardless of which root the user is browsing — `QueryTabView` calls `runRawQuery(sql)` with no id and `ChatTabView` has no `connectionId` at all, so neither can ACQUIRE a connection today (the tree only activates tables). `WorkspaceTab.connectionId`/`WorkspaceSnapshotTab.connectionId` are deliberately generic so wiring them later is additive: it needs a per-tab target picker (or a "new query against this connection" tree affordance), not a schema change. Note that report tabs already have their own independent picker (Story 6.2) and are NOT part of this gap. -- Rationale: the epic's Story-10.6 AC text says "table/query tab" while the solution point and the only bound-ref type cover table tabs only; the ledger is where that residue is recorded instead of being silently dropped.

**Acceptance Criteria:**

- Given a table tab bound to a saved connection and a workspace save, when the app is relaunched and `workspace.load` restores it, then the tab reappears with the SAME `connectionId`, still unbound (`table === undefined`, Story 3.2 intact), in the same strip position with the same title/kind/id — and `WORKSPACE_SNAPSHOT_VERSION` is still `1`.
- Given a workspace whose table tabs are all boot-bound or never bound (the ephemeral single-connection case), when it is serialized, then NO tab carries a `connectionId` key at all, so the snapshot is byte-identical to a pre-10.6 one and the untouched-workspace no-resave invariant holds.
- Given a restored tab whose `connectionId` is absent from `connections.list`, when the workspace renders, then that tab shows the "conexión no disponible" body with the reassign affordance while the tab strip, every sibling tab, the schema tree and the status bar are untouched — and no exception is thrown at any point of `workspace.load` → `restoreWorkspace` → render.
- Given the live connection set is not yet known (the `connections.list` fetch is in flight or failed), when a table tab with a `connectionId` renders, then it renders its NORMAL body — the unavailable state is never shown on an unresolved or failed registry read.
- Given a connection is removed in Settings while a tab bound to it is open, when the mutation succeeds, then `registryRevision` bumps, `connections.list` is re-read and that tab flips to "conexión no disponible" without an app restart, while every other tab keeps its state.
- Given the user reassigns an unavailable tab to a live connection, when the picker selection is committed, then the tab's `connectionId` becomes the chosen id, its stale `table` binding is cleared, the normal "select a table" body returns, and the new id is what the next `workspace.save` persists.
- Given `bunx tsc --noEmit`, `bun test` and `bun run build`, when run, then all three pass, the suite grows only additively (plus the ONE rewritten 10.5 test named in the tasks), and no other existing assertion is weakened or deleted.

## Design Notes

**The Block-If resolved as predicted — the pure module keeps the decision, the render layer keeps the RPC.** `isTabConnectionMissing(tab, liveIds)` takes the live id set as a plain `ReadonlySet<string> | null` parameter, so `workspace-state.ts` gains no import, no RPC and no DOM dependency while still owning the truth table (including the deliberate "unknown set ⇒ never flag" rule). `Workspace.tsx` owns the only new `rpc` call. That split is also what makes the availability logic testable at all — `renderToStaticMarkup` never runs effects, so a decision made inline in the component would be unverifiable.

**Why `null` is never written to disk.** The tab-level field is `string | null | undefined` in memory, but `toWorkspaceSnapshot` emits it only when `!= null` — exactly the `lastProvider` line it sits next to. `null` and `undefined` are read identically everywhere (boot/default target), so writing `null` would add bytes without adding information, and it would break the byte-identical guarantee for the ephemeral single-connection case, where EVERY table tab is boot-bound. The snapshot type still accepts `null` because a hand-written or future file may contain it and both boundaries must tolerate it.

**Strict at save, tolerant at load — and why "tolerant" here means *ungated*.** `checkTabs` is the trusted write boundary and already rejects a bad `id`/`kind`/`title`, so a bad `connectionId` belongs in the same list. The store's `isWorkspaceSnapshot`, by contrast, is all-or-nothing over `tabs`: a per-field check there would turn one hand-edited value into a wiped workspace. So the field is not gated at the store at all, and `restoreWorkspace` (which is already the "pure and total" defense against hand-edited files, and already the home of `restoreLastProvider`'s unknown → `null` sanitize) drops a non-string value on the way in. Same shape as Story 8.5, same reasoning, one less special case to remember.

**Query tabs are out — resolving the draft's open question.** The epic's AC text says "table/query tab", but no query tab can ACQUIRE a `connectionId` today: `QueryTabView` calls `runRawQuery(sql)` with no id, the schema tree only ever activates *tables*, and there is no per-tab target picker outside Report tabs' own Story-6.2 mechanism. Persisting a field nothing can populate would be dead weight, so this story wires table tabs only — while keeping `WorkspaceTab.connectionId`/`WorkspaceSnapshotTab.connectionId` generic over tab kind so the follow-up (DW-70) is purely additive: it needs a picker, not a schema change.

**The unavailable body replaces `SelectTablePrompt` too, not just `TableTabView`.** A restored tab is unbound by definition (3.2), so the interesting case — the one the AC describes — has `table === undefined` and would otherwise render "select a table", quietly implying the tree can fix it. The swap is therefore keyed on the TAB's `connectionId`, not on the bound ref, and covers both branches.

## Verification

**Commands:**
- `bunx tsc --noEmit` -- expected: no diagnostics; in particular `WorkspaceSnapshotTab`'s new optional field must not break `toWorkspaceSnapshot`'s type-guard filter, and the new `WorkspaceAction` member must be exhaustively handled in `workspaceReducer`'s `switch`.
- `bun test` -- expected: full suite green, additive except the one rewritten 10.5 non-persistence test; baseline at `4ad9393` is `1485 pass / 0 fail across 75 files`, so the new total must be strictly higher with 0 failures.
- `bun run build` -- expected: all four build scripts (ui / sandbox / snapshot / live-report) complete without error.
- `grep -n "red-400\|red-500\|amber-" src/ui/workspace/TabContent.tsx` -- expected: only the pre-existing `:409-438` row-fetch / mutation banners match; the new `ConnectionUnavailable` body adds no raw color literal.
- `grep -n "connectionId" src/core/workspace-store.ts` -- expected: matches only inside the new explanatory comment, never inside `isSnapshotTab`'s returned boolean expression.

**Manual checks (if no CLI):**
- Read `toWorkspaceSnapshot`'s tab `.map` and confirm the spread is guarded by `!= null` (not `!== undefined`), so a boot-bound tab serializes without the key.
- Read `restoreWorkspace`'s tab loop and confirm the sanitize accepts ONLY a non-empty string, that the key is omitted (not set to `null`) otherwise, and that the dedupe/settings-singleton passes and the `nextId`/`activeTabId` reconciliation below them are untouched.
- Read `isWorkspaceSnapshot` and confirm `connectionId` is referenced nowhere in it — the load boundary must stay total.
- Read the `table` branch of `TabContent` and confirm the unavailable swap sits ABOVE the `tab.table !== undefined` ternary, so it covers the bound and unbound cases alike, and that `liveIds === null` falls through to the normal render.

## Spec Change Log

<!-- Append-only. Populated by step-04 during review loops. Empty until the first bad_spec loopback. -->

## Review Triage Log

<!-- Append-only. Populated by step-04 on every review pass. Empty until the first review pass. -->

### 2026-07-22 — Review pass

- intent_gap: 0
- bad_spec: 0
- patch: 9: (high 0, medium 3, low 6)
- defer: 5: (high 0, medium 1, low 4)
- reject: 11: (high 0, medium 0, low 11)
- addressed_findings:
  - `[medium]` `[patch]` The reassign picker had no path back to the boot/default target: `setTabConnection` and the reducer action accept `string | null`, but the UI chain (`Workspace`/`TabContent`/`ConnectionUnavailable`) narrowed it to `string`, so a workspace saved against `conn-2` whose saved connections were all deleted, relaunched with a boot `--url`, showed the DISABLED button plus "no hay conexiones guardadas" with a usable boot connection sitting right there. Widened the three signatures to `string | null`; `Workspace`'s effect now reads `connection.active` alongside `connections.list` in one `Promise.all` and threads `hasBootTarget` (the `ActiveConnectionInfo.hasTarget` predicate 10.5 added) down; the picker renders a `conexión por defecto (boot)` entry above the saved list emitting `null`, and the inert branch now needs BOTH no boot target and no saved connection.
  - `[medium]` `[patch]` A hand-edited registry could yield `id: ""` (the store checks `typeof` only); reassigning to it would write `connectionId: ""` and `checkTabs` would then reject EVERY `workspace.save` for the rest of the session, silently. `setTabConnection` now normalises a blank id to `null` before writing, with a unit test.
  - `[medium]` `[patch]` Several new assertions could not fail — the AR-12 credential test was a tautology over `ConnectionSummary` (which has no url/user/password by construction), `toContain("orders")` passed on any echo including a class or key, `not.toContain("analytics")` was trivially true for an empty list, the "zero rpc" claim held for any effect-based component under `renderToStaticMarkup`, `not.toContain("red-")` was a substring proxy, and the rewritten non-persistence test leaned on the fixture's schema name (`not.toContain("public")`). Replaced each with a falsifiable assertion (opaque ids absent while name/host present, the title matched inside its own element, a button count, `text-err` asserted positively, the emitted tab's exact key set) and added the missing coverage for the one real integration point: `TabContent` itself now has tests for id-absent → unavailable body, id-present → normal body, and `connections={null}` → normal body. The store test also now asserts `tabs[0]`, the only tab that could falsify its own claim.
  - `[low]` `[patch]` The `<details>` disclosure state leaked between tabs (`TabContent` renders only the active tab, so two unavailable tabs reused one element) — added `key={tab.id}`.
  - `[low]` `[patch]` The reassign controls rendered enabled while being no-ops when the optional handler was absent anywhere in the `App → Workspace → TabContent → ConnectionUnavailable` chain — a missing handler now renders the disabled control instead.
  - `[low]` `[patch]` The body's `lowercase` container mangled the copy it claimed to render verbatim (`Reasignar conexión…` → `reasignar conexión…`) and re-cased the tab title, which IS the table name (AR-19) — added `normal-case` to the title line and every reassign control.
  - `[low]` `[patch]` `<summary className="list-none">` still draws the disclosure triangle on WebKit — added `[&::-webkit-details-marker]:hidden` (verified in the built CSS).
  - `[low]` `[patch]` A failed initial `connections.list` disabled the whole feature for the session (the effect wrote only on `ok`, and only a Settings mutation could re-trigger it) — added exactly one bounded 2s retry, cleared by the effect cleanup, with the "unknown set ⇒ never flag" rule untouched.
  - `[low]` `[patch]` Two doc comments stated things that were not true: the copy was called "the mockup's, verbatim" when the layout is a two-element tab-body adaptation of the mockup's one tree line, and the `SelectTablePrompt` swap was justified by "the tree cannot fix it" when clicking any surviving root's table does fix it via `bindTableToActiveTab`. Both rewritten to the real reasons.
  - deferred (5, appended to `deferred-work.md`): the persisted id is observable only in the failure case (no per-tab connection identity anywhere); editing a saved connection's URL keeps its id live so a bound tab silently reads/writes the old table name against the new database (pre-existing since 10.4/10.5); `connections.list` now has three independent owners whose two `registryRevision`-keyed copies can silently disagree; a broken background tab is invisible until activated, plus a select-a-table → unavailable flash on relaunch; and `src/core/workspace-store.test.ts` carries pre-existing NUL bytes that make every diff of it read `Bin N -> M bytes`.
  - rejected (11): reassignment being overwritten by the next tree click (spec-conformant — the tab is unbound by design); a non-array `ok` payload crashing `.map` (trusted in-process Core, and the idiom every sibling consumer already uses); duplicate registry ids; non-table tabs being *allowed* to carry an id (deliberately generic per DW-70); a no-op identity check in `setTabConnection`; `load()` returning values that violate the declared type and never self-healing them (the documented `lastProvider` posture this story deliberately mirrors); the "FIELD-DROP" wording (the repo's own vocabulary for exactly this); the two in-memory encodings of the boot target (`null` vs an omitted key — documented, and every read uses `== null`); the unreachable `connections ?? []` fallback; and the `<summary>`-vs-`<button>` semantic split (now rarely reachable after the boot entry landed).

### 2026-07-22 — Review pass (follow-up)

- intent_gap: 0
- bad_spec: 0
- patch: 5: (high 0, medium 1, low 4)
- defer: 0
- reject: 9: (high 0, medium 0, low 9)
- addressed_findings:
  - `[medium]` `[patch]` The previous pass's blank-id guard was bolted onto the SECONDARY writer only: `setTabConnection` normalised `""` to `null`, but `bindTableToActiveTab` — the primary writer, the one a tree click goes through — still did `ref.connectionId ?? null`, which does not catch `""`. The chain was intact end to end (`isStoredConnection` accepts any string id → `savedDescriptor` copies it → `onActivate` → the ref → the tab → `toWorkspaceSnapshot` emits it because `"" != null` → `checkTabs` rejects it), so every autosave for the rest of the session would fail with `bad_request` and no UI surface. Worse, the two ends disagreed on what "blank" means: `checkTabs` accepted `"   "` while `setTabConnection` rewrote it to the boot target, so a tab could silently retarget a database the user never picked. Extracted ONE `normalizeConnectionId` predicate (blank ⇒ `null`) used by `bindTableToActiveTab`, `setTabConnection` and `restoreWorkspace`'s on-read sanitize, and tightened `checkTabs` to the same non-BLANK rule so both boundaries agree. Covered by new cases in `workspace-state.test.ts` (both bind branches, three blank shapes) and extended bad-value loops in `workspace-registry.test.ts` and the restore sanitize test.
  - `[low]` `[patch]` `Workspace`'s retry budget was once per APPLICATION LIFETIME, not once per `registryRevision` as its own comment claimed: `setListAttempt(1)` was the only writer, so after the first failure the `listAttempt === 0` gate could never be true again and a `connections.list` failure at any later registry mutation got no retry, leaving a just-deleted connection rendering as live indefinitely. Replaced the bare counter with `retriedRevision` (the revision whose retry was already spent), which self-resets per revision because the comparison is against the current one.
  - `[low]` `[patch]` The retry was armed only by a failed `connections.list`, never by a failed `connection.active` — while `hasBootTarget` starts `false` and is only ever raised on `ok`, so it conflates "unknown" with "definitely none". A `connections.list` returning `[]` alongside a failed `connection.active` therefore produced verbatim the dead end the second read was added to prevent: a disabled "Reasignar conexión…" plus "no hay conexiones guardadas" with a usable boot target sitting right there, unrecoverable for the session. Either half failing now arms the one retry.
  - `[low]` `[patch]` `ConnectionUnavailable`'s picker list was unbounded inside an `h-full … justify-center` body, so with enough saved connections it spilled off BOTH edges — and an ancestor's `overflow-auto` cannot scroll to a negative offset, making the alert line itself unreachable. Bounded the `<ul>` with `max-h-48 overflow-y-auto` (verified present in the built CSS).
  - `[low]` `[patch]` `App.tsx`'s `primaryKeys` bail carried a forward reference naming THIS story as the thing that lifts it ("Story 10.6 makes the per-connection catalog available and lifts this") — but 10.6 shipped and the spec's own task list froze that bail exactly as 10.5 left it, so the comment now pointed a future reader at a completed story for a fix that does not exist. Retargeted at the deferred-work ledger entry that actually owns it.
  - deferred (0): nothing survived triage as a genuinely new pre-existing issue — the duplicated `connections.list` ownership raised again this pass is already covered by the entry the previous pass appended.
  - rejected (9): the duplicated registry reads between `Workspace` and `SchemaTree` (already in the ledger from the previous pass; re-filing it would be churn); a dangling `connectionId` on a non-table tab never being flagged (nothing can write one — deliberately generic per DW-70, rejected on the same grounds last pass); the `<summary>`-vs-`<button>` semantic split and the unsound `isSnapshotTab` type predicate (both explicitly rejected last pass, with unchanged reasoning); the inert branch showing no hint when the optional handler is absent (defensive code for a state the props chain makes unreachable); a freshly-allocated `Set` per render for one membership test (micro-allocation the existing comment already addresses); two test substring proxies (`not.toContain("red-")` matching a hypothetical `shared-` host) that cannot misfire on this file's fixed fixtures; the removal of a whole-snapshot "no connection id anywhere" assertion (its premise is exactly what this story reverses); refusing rather than normalising a whitespace-only reassign target (resolved coherently by the single predicate above); and the process observation that the previous pass's patches outgrew the frozen task list without a Spec Change Log entry (patches do not trigger a spec amendment, and the Review Triage Log already records every one of them).

## Auto Run Result

Status: `done`

### Implemented change

Tabs now remember which connection they belong to, and survive that connection being removed. `WorkspaceTab` gained a top-level `connectionId?: string | null` — independent of the session-only `table` ref, mirrored from `ref.connectionId` on every bind — and `WorkspaceSnapshotTab` gained the matching ADDITIVE optional field, so the id survives a relaunch with `WORKSPACE_SNAPSHOT_VERSION` still `1` and a boot-only workspace still serializing byte-identically (the field is written only when it is a real saved-connection id, mirroring `lastProvider`). Story 3.2 is untouched: the schema/name binding still never persists, so a restored tab reopens unbound but *knowing its database*. The save boundary rejects a malformed `connectionId` outright; the load boundary deliberately does not gate it (that guard is all-or-nothing over `tabs`), with the sanitize living one ring away in the pure `restoreWorkspace`. At the render layer, `Workspace` owns the live saved-connection set (re-read on every registry mutation, plus a one-shot retry) and a table tab whose id is absent from it renders a "conexión no disponible (fue eliminada)" body with a working "Reasignar conexión…" picker — including a boot/default entry — in place of the normal body, never dropping the tab or touching a sibling.

### Files changed

- `src/shared/contract.ts` — `WorkspaceSnapshotTab.connectionId` added as an additive optional field, documented alongside the `erdLayouts`/`lastProvider` family; no version bump.
- `src/ui/workspace/workspace-state.ts` — `WorkspaceTab.connectionId`; `bindTableToActiveTab` mirrors `ref.connectionId ?? null` in both branches; `toWorkspaceSnapshot` emits it only when non-null; `restoreWorkspace` carries and sanitizes it; new `setTabConnection` (clears the stale `table`, normalises a blank id) and the pure `isTabConnectionMissing` predicate.
- `src/core/workspace-registry.ts` — `checkTabs` strictly validates a present `connectionId` (`null` or non-empty string) at the trusted save boundary.
- `src/core/workspace-store.ts` — `isSnapshotTab` left deliberately ungated for the new field, with the rationale documented in the `lastProvider` voice.
- `src/ui/workspace/Workspace.tsx` — owns the live `ConnectionSummary[]` + `hasBootTarget`, fetched in one `Promise.all` keyed on `registryRevision` with a bounded retry, and threads them plus `onReassignConnection` into `TabContent`.
- `src/ui/workspace/TabContent.tsx` — the exported presentational `ConnectionUnavailable` (mockup copy, tokenized, `<details>` picker with a boot entry) and the availability swap above the bound/unbound ternary in the table branch.
- `src/ui/App.tsx` — the `setTabConnection` reducer action and the `onReassignConnection` dispatch.
- Tests — `workspace-state.test.ts` (+6 describes, one authorised rewrite of the 10.5 non-persistence test), `workspace-registry.test.ts` (accept/reject + verbatim pass-through), `workspace-store.test.ts` (round-trip, pre-10.6 load, malformed value still loads), and the NEW `TabContent.test.tsx` (14 cases over the unavailable body and the swap).
- `_bmad-output/implementation-artifacts/deferred-work.md` — DW-70 plus the five review defers.

### Review findings

- Patches applied: 9 (3 medium, 6 low) — see the triage log above.
- Deferred: 5 (1 medium, 4 low).
- Rejected: 11.
- No intent gaps and no bad-spec loopbacks: the implementation matched the frozen contract on the first pass, and both Block-If conditions resolved as the contract predicted (the availability check stayed out of the pure reducer; 10.5's activation callback already carried the id).

**Follow-up review pass (2026-07-22), independent Blind Hunter + Edge Case Hunter over the same `4aad393..worktree` diff:**

- Patches applied: 5 (1 medium, 4 low) — the blank-`connectionId` predicate unified across all four seams that read or write the field (the previous pass had guarded only the reassign path, leaving the primary tree-click writer able to poison every autosave); `Workspace`'s retry budget made genuinely per-`registryRevision` and armed by EITHER failed registry read, not just the list; the picker list bounded and scrollable; and a stale forward reference in `App.tsx` naming this story as lifting a bail it deliberately left alone.
- Deferred: 0 — nothing new survived triage; the one pre-existing issue re-raised is already in the ledger from the previous pass.
- Rejected: 9, five of them re-raises of findings the previous pass had already rejected with reasoning that still holds.
- Still no intent gaps and no bad-spec loopbacks; `review_loop_iteration` stays `0`.

### Verification

- `bunx tsc --noEmit` — no diagnostics, exit 0 (re-run after the follow-up patches).
- `bun test` — `1526 pass / 0 fail / 4077 expect() calls across 76 files` (baseline at `4ad9393`: 1485 across 75). The follow-up pass added one new test plus extra cases inside three existing bad-value loops; nothing was weakened or deleted.
- `bun run build` — build-ui / build-sandbox / build-snapshot / build-live-report all wrote their bundles (re-run after the follow-up patches).
- `grep -c "max-h-48\|overflow-y-auto" src/core/ui-bundle.generated.ts` — 2; the picker's scroll bound really reached the built CSS, not just the JSX.
- `grep -n "red-400\|red-500\|amber-" src/ui/workspace/TabContent.tsx` — only the pre-existing row-fetch/mutation banners match; the new body adds no raw color literal.
- `grep -n "connectionId" src/core/workspace-store.ts` — matches only inside the explanatory comment, never inside `isSnapshotTab`'s returned expression.

### Residual risks

- `Workspace`'s new effect (the `Promise.all`, the `!ok`-keeps-previous rule and the now per-revision retry) has no test — the repo has no jsdom, so effects are unreachable from `renderToStaticMarkup`; it is verified by reading only. The follow-up pass changed this effect twice (the retry's budget key and its arming condition), so it is the one surface where the review's own fixes rest entirely on inspection.
- `ConnectionUnavailable`'s `max-h-48` scroll bound is asserted only in the built CSS, not in a layout test — the repo cannot render layout, so an overflow regression would again be caught by reading rather than by the suite.
- The live-set fetch now duplicates one `SchemaTree` already makes; if the two disagree (one read fails, the other succeeds) the unavailable check silently stays off with no signal. Deferred.
- A tab restored with a still-valid `connectionId` surfaces that fact nowhere in the UI, so the story's value is observable mainly in the failure case. Deferred.
- The malformed-value path is tolerated on every launch rather than self-healed on disk (the documented `lastProvider` posture), so a hand-edited file keeps its bad bytes indefinitely.
