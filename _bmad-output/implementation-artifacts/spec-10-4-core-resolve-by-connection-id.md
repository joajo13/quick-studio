---
title: 'Core resolves every read path (connect, table.rows, chat) by connectionId via connectionTargets'
type: 'feature'
created: '2026-07-21'
status: 'done'
baseline_revision: 'b304d6c61814c4c3d731ffc5727cd7e5320011d3'
final_revision: '05006578b849edc21a83da44dc8c209b62fb8f44'
review_loop_iteration: 0
followup_review_recommended: true
context:
  - '{project-root}/_bmad-output/planning-artifacts/epics.md'  # Epic 10 / Story 10.4
  - '{project-root}/_bmad-output/planning-artifacts/epic-10-multi-connection-tree.mockup.html'
warnings: ['oversized']
---

<intent-contract>

## Intent

**Problem:** quick-studio is clamped to ONE live connection per session. `startCore` builds a single boot `connectionManager` (`src/core/connection.ts`) over a closure-captured URL, and three read paths reach into it DIRECTLY: `tableRows` (`src/core/server.ts`), the `connect`/`connection.active`-adjacent schema path, and the chat responder's `getSchema` seam (`src/core/chat.ts` via `createChatResponder({ getSchema: () => connectionManager.getSchema() })`). Meanwhile a per-target resolver already exists (`src/core/connection-targets.ts`, Story 6.2) — a lazy, cached, registry-self-invalidating manager keyed by an opaque saved-connection `id` — but today ONLY the Reports executor (`src/core/executor.ts`) consumes it. Everything else cannot browse a saved connection other than the boot one, which blocks the multi-root tree (10.5) and connection-aware tabs (10.6).

**Approach:** Propagate the SAME `connectionTargets.resolve(connectionId)` seam the executor already uses to the three remaining read paths, changing NOTHING about the pool itself:
1. **`connection-targets.ts`** — widen `ConnectionSeams` with one more 1:1 delegation, `connect: () => manager.connect()` (mirrors the existing `runQuery`/`runReadOnly`/`getEngine`/`getSchema`/`quoteIdent` pattern in `seamsFor`), so a resolved target exposes the SAME neutral `ConnectResult` (`status: "connected" | "failed"`) the boot manager already returns — no new failure-shape logic.
2. **`server.ts`** — `tableRows(params)` extracts an optional `connectionId` from `params` (mirroring the executor's shape-validate-then-`resolveConnection` block at `executor.ts:680-693`), calls `connectionTargets.resolve(connectionId)` instead of touching `connectionManager` directly, and runs `planTableRows`/`query`/`quoteIdent` against the resolved `ConnectionSeams`. The `connect` RPC context capability becomes `connect(connectionId)`, resolving the target and delegating to `seams.connect()`. The chat responder's `getSchema` dependency becomes `getSchema: (connectionId) => resolve-then-seams.getSchema()`.
3. **`chat.ts`** — `ChatResponderDeps.getSchema` widens to accept the optional `connectionId` already threaded from `answerStream`'s `params`; the existing "no active connection" `bad_request` catch (`prepareRequest`, `chat.ts:276-281`) is reused verbatim for a resolve failure — no new chat-side error code.
4. **`rpc.ts`/`contract.ts`** — `connect` becomes a `preformed` handler (like `connections.*`/`execute`) so it can shape-check `connectionId` (string-or-null) BEFORE any round-trip and map a resolve failure to a typed error envelope, exactly like `executor.ts`'s `targetError`. `TableRowsRequest` gains `connectionId?: string | null`, mirroring `ExecuteRequest`'s existing field.

The untargeted path (`connectionId` omitted or `null`) must stay byte-identical: `connectionTargets.resolve(null)` already returns the boot manager's seams (`connection-targets.ts:119-121`), so every existing call site that never sends an id keeps its current behavior verbatim.

## Boundaries & Constraints

**Always:**
- AR-12: only the opaque `connectionId` crosses the loopback RPC; the URL/user/password are resolved in Core via `connectionTargets.resolve(id)` and NEVER echoed to the client.
- `connectionId` omitted or `null` at every one of the three seams falls back to the BOOT manager (the existing default target), so Ephemeral mode and every currently-green test stay unchanged.
- Reuse the EXISTING `connection-targets.ts` pool as-is (lazy, cached-by-id, self-invalidating against the registry, closed in `closeAll()`); the only change to that file is the additive `connect` seam.
- Mirror the executor's established pattern exactly: shape-validate `connectionId` (string or null) BEFORE any connection round-trip → `bad_request` on a wrong type; map a resolve failure via the same `not-found → not_found` / `unavailable → internal_error` convention (`executor.ts:434-438`).
- `table.rows` and `connect` accept `connectionId` the same way `execute` already does (`ExecuteRequest.connectionId?: string | null`) — same field name, same semantics, for a consistent wire contract across all targeted RPCs.

**Block If:**
- `connect`/`table.rows` cannot accept an optional `connectionId` without changing the reply shape or behavior of the existing no-id call sites — HALT `blocked`, condition `connectionId threading breaks the byte-identical ephemeral/default path`. (Expected FALSE: `connectionTargets.resolve(null)` already returns the boot manager's seams unchanged.)
- The resolved target's neutral `ConnectResult` cannot be produced from `ConnectionSeams` without duplicating `connection.ts`'s driver-open/failure-classification logic outside `connection.ts` — HALT `blocked`, condition `no seam exists to produce a neutral connect result for a resolved target`. (Expected FALSE: every cached/target manager IS a full `ConnectionManager`, so `seamsFor` can delegate `connect` to `manager.connect()` directly, same as every other seam.)

**Never:**
- Never send a URL, username, or password over `/rpc` or `/chat/stream` — only the id.
- Never build a second/parallel connection pool or bypass `connectionTargets` — `tableRows`/`connect`/chat resolve through the SAME `resolve(connectionId)` the executor uses.
- Never change the behavior, reply shape, or test expectations of the untargeted (no `connectionId`) path for `connect`, `table.rows`, or chat.
- Never let a targeted chat request change the outbound provider payload's schema-only invariant (`rowSample: null`, AR-6/R5) — `connectionId` only changes WHICH schema is introspected, never what leaves the machine.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| `connect` with no `connectionId` | `params` omits it / `connectionId: null` | Resolves the BOOT manager exactly as today; `ConnectResult` unchanged, byte-identical | none |
| `connect` with a valid saved-connection id | `connectionId: "<id>"` for a saved connection | `connectionTargets.resolve(id)` opens/reuses the cached target manager; `seams.connect()` returns the neutral `ConnectResult` (`connected` or a classified `failed`) for THAT target | domain `failed` status, not a throw |
| `connect`/`table.rows` with an unknown id | `connectionId` no longer in the registry (removed) | `resolve` → `not-found` → `errorReply("not_found", "no connection with that id")` | typed `not_found` envelope |
| `connect`/`table.rows` during a credential-store blip | `resolve` → `unavailable` | `errorReply("internal_error", "credential store is unavailable")` | typed `internal_error` envelope, no store details leaked |
| `connect`/`table.rows` with a malformed `connectionId` | `connectionId` present but not a string/null (e.g. a number) | Rejected BEFORE any round-trip: `bad_request` | typed `bad_request` envelope |
| `table.rows` targeting a saved connection | valid `connectionId`, valid table/page params | Rows composed/paginated against the RESOLVED target's schema (via `seams.getSchema`/`seams.quoteIdent`/`seams.runQuery`), identical shape to the boot-manager path | validation errors unchanged (`bad_request`/`not_found` on bad table/column) |
| Chat with a targeted `connectionId` | valid id, provider configured | `getSchema(connectionId)` resolves the target and introspects THAT schema; outbound payload stays schema-only (`rowSample: null`) exactly as today, just scoped to a different DB | same redacted `error` chunk path on failure |
| Chat with an unresolvable `connectionId` | unknown/unavailable id | Reuses the EXISTING "no active connection" `bad_request` catch (`chat.ts:276-281`) — a resolve failure surfaces the same way a boot-connect failure already does; no new chat error code | `bad_request`, "no active connection" |
| Connection repointed/removed in Settings mid-session | a cached target's stored url changes or the connection is deleted | `connectionTargets`'s existing self-invalidation fires on the NEXT `resolve` (no change needed here — reaffirmed, not re-implemented) | stale manager evicted+closed automatically |
| Shutdown races a targeted resolve | `stop()` in flight when `connect`/`table.rows`/chat resolves | `connectionTargets` is latched closed → `not-found`, never opens a leaked target (existing `connection-targets.ts` behavior, reused as-is) | typed `not_found` |
| Ephemeral boot, no `connectionId` ever sent | positional `databaseUrl` boot, UI never sends an id | Every existing call site and test is UNCHANGED — this story adds a capability, it does not require the UI to use it yet | none |

</intent-contract>

## Acceptance Criteria

- **Given** the read RPCs `connect` and `table.rows`, **when** they are called with an optional `connectionId`, **then** the Core resolves the live manager via `connectionTargets.resolve(connectionId)` — instead of touching the boot `connectionManager` directly — for `tableRows`, `connect`/`getSchema`, and the chat responder; only the opaque id crosses the loopback (AR-12), and the URL is resolved in Core.
- **Given** an RPC or chat request with no `connectionId` (or `connectionId: null`), **when** it resolves, **then** it falls back to the boot manager as the default target, so Ephemeral mode (positional URL) is completely unchanged and every existing test stays green.
- **Given** a `connectionId` that names a saved connection, **when** `connect` or `table.rows` is called with it, **then** the response reflects THAT connection's schema/rows, not the boot connection's — reusing the SAME cached-by-id, self-invalidating pool the Reports executor already relies on.
- **Given** a `connectionId` that is unknown, removed, or hits a store blip, **when** any of the three seams resolves it, **then** the failure is typed and credential-neutral (`not_found` / `internal_error` for RPCs; the existing "no active connection" `bad_request` for chat) — never a raw URL/credential, never an unhandled throw for a malformed id.
- **Given** DW-45, **when** a schema-mutating `execute` succeeds against a resolved target, **then** the NEXT `getSchema`/`connect` for THAT target (boot included) re-introspects instead of serving the memo, and no other target's memo is touched.
- **Given** the full suite, **when** run after this change, **then** `bunx tsc --noEmit` is clean, `bun test` is green with NO existing test needing a behavior change (only new tests for the targeted paths, plus the mechanical stub updates named in Design Notes), and `bun run build` succeeds.

## Code Map

> Line references verified against `HEAD` = `b304d6c` (Story 10.3 landed).

- `src/core/connection.ts:41-83` — the `ConnectionManager` interface (`connect`/`getSchema`/`query`/`queryReadOnly`/`quoteIdent`/`describe`/`close`). `:112-119` closure state (`driver`, `cached`, `inflight`, `closed`); `:135-162` `open()` (sets `cached = {status:"connected", schema}` at `:150` after `d.listSchema(pinnedSchema)`); `:165-193` `doConnect()` (idempotent early-return `if (cached !== null) return cached` at `:167`); `:201-216` `ensureDriver()` (returns the open driver WITHOUT calling `doConnect` at `:203`); `:221-229` `getSchema()` (memoized read, defensive re-introspect branch at `:226-228`). **No invalidation seam exists** — this is DW-45's root cause.
- `src/core/connection-targets.ts:40-46` — `ConnectionSeams` (5 members today). `:64-66` `ResolvedConnection` = `{ok:true, seams}` | `{ok:false, reason:"not-found"|"unavailable", detail?}`. `:93-101` `seamsFor(manager)` — the 1:1 delegation table the two new members join. `:134-171` `resolve(id)`; `:136-138` the `null`/`undefined` → boot-seams branch (the byte-identical default path); `:140-142` the closed-latch → `not-found` branch.
- `src/core/executor.ts:429-439` — `createExecutor` + the private `targetError(reason)` to be replaced by the shared export. `:455-490` `executeRaw` (read branch `:473-478`, `confirmation_required` `:482-487`, confirmed-mutation branch `:488-489`). `:494-505` `executeStructured` (`insert`/`update`/`delete`/`createTable`). `:576-642` `executeCreateTable`. `:670-715` `execute` — the pattern to mirror: shape checks before any round-trip (`:673-686`, `connectionId` at `:683-686`), `resolveConnection` + `targetError` inside the try (`:698-699`, `:706-707`), `NoConnectionTargetError` → `bad("no active connection")` at `:712`.
- `src/core/server.ts:305-327` — `tableRows(params)`: `connectionManager.getSchema()` `:307`, `planTableRows(schema, params, quoteIdent)` `:308`, `connectionManager.query` `:313`/`:315`, the `NoConnectionTargetError` catch `:321-326`. `:335-340` `createConnectionTargets({bootManager, getStoredUrl, createManager})`. `:349-351` the executor wiring (the precedent). `:357-361` `createChatResponder({getSchema: () => connectionManager.getSchema(), ...})`. `:439-461` `rpcContext` — `connect: () => connectionManager.connect()` at `:445`, `activeConnection` at `:448` (**untouched by this story**). `:535-564` the `/chat/stream` POST handler — the parsed JSON body rides through as `params` untouched, so a `connectionId` field already reaches `answerStream` today.
- `src/core/rpc.ts:37-90` — `RpcContext`; `connect` typed `() => Promise<ConnectResult>` at `:40`, `tableRows` at `:66` and `execute` at `:75` are the `(params: unknown) => Promise<RpcReply<T>>` precedent. `:95-110` the `PREFORMED` symbol + `preformed()`. `:149` the current `connect` handler. `:257`/`:265` the `table.rows`/`execute` preformed handlers to copy. `:297-332` `dispatch` (per-handler param validation; any throw → generic `internal_error`).
- `src/shared/contract.ts:324-330` `ConnectResult`; `:344-349` `TableRowsRequest` (no `connectionId` today); `:382-391` the `ConnectionSummary.schema` doc that names Story 10.4 as pending (**must be updated**); `:901-903` `ExecuteRequest` (the `connectionId?: string | null` precedent); `:929-936` `RpcErrorCode`; `:992-1006` `RpcReply`/`okReply`/`errorReply` (`errorReply` returns `RpcReply<never>`).
- `src/core/chat.ts:190-199` `ChatResponderDeps` (`getSchema: () => Promise<DatabaseSchema>`); `:213` `answerStream(params: unknown, signal?)`; `:237-303` `prepareRequest` (provider check `:248-251`, message check `:254-257`, the `getSchema` try/catch `:276-281`, `assemblePayload` call `:291`); `:129-136` `assemblePayload` — the `rowSample: null` invariant (**untouched**).
- Tests: `src/core/connection.test.ts:146-153` (typed no-target throws); `src/core/connection-targets.test.ts:21-74` (`fakeManager`/`harness` factories); `src/core/executor.test.ts:61-80` (`makeSeams`), `:914-1071` (`makeTargetedExecutor` + the targeted-routing describe — the precedent to mirror), `:1005`/`:1042` (two inline `ConnectionSeams` literals); `src/core/server.test.ts:145-163` (untargeted `table.rows`), `:376`/`:410`/`:452-453` (untargeted `connect`); `src/core/rpc.test.ts:112-164` (`stubCtx`), `:192-209` (verbatim `methodNames()` list), `:219-246` (`connect` dispatch); `src/core/chat.test.ts:77-97` (`makeResponder`), `:354-364` (the "no active connection" test).
- UI call sites — **all untouched by this story** (they send no id and must keep working verbatim): `src/ui/schema/SchemaTree.tsx:137` `rpc("connect")`; `src/ui/workspace/TabContent.tsx:144` `rpc("table.rows", {schema, table, page})`; `src/ui/rpc/rpc-stream.ts:60-76` `streamChat` (body `{provider, message}`).

## Tasks & Acceptance

**Execution:**

- [x] `src/core/connection.ts` -- add `invalidateSchema(): void` to the `ConnectionManager` interface and implement it in `createConnectionManager`: a `schemaStale` flag plus a single-flight private `refreshIfStale(d: Driver)` that re-runs `d.listSchema(pinnedSchema)` and REPLACES `cached` with `{status:"connected", schema}` before clearing the flag. Call it from `getSchema()` (after `ensureDriver()`, before reading `cached.schema`) and from `doConnect()`'s idempotent `cached !== null` branch (re-reading `cached` AFTER the await, so the fresh object is returned). `invalidateSchema()` must be a pure flag set — never throw, never open a driver, no-op when never connected or closed. In `doConnect`'s branch ONLY, a `DriverConnectionError` out of the refresh is caught and returned as the neutral `{status:"failed", failure, message}` `open()` already produces, leaving `cached` intact and `schemaStale` true (retryable); `getSchema()` lets it propagate, matching its existing throw-on-unusable-connection contract -- this is DW-45's root fix: the memo is per-manager and today only a `close()` clears it, which is unusable for the boot manager.
- [x] `src/core/connection-targets.ts` -- add TWO 1:1 delegations to `ConnectionSeams` and `seamsFor` (`:40-46`, `:93-101`), keeping the existing member order/comment style: `connect: () => manager.connect()` and `invalidateSchema: () => manager.invalidateSchema()`. Also export `targetError(reason: "not-found" | "unavailable"): RpcReply<never>` returning `errorReply("not_found", "no connection with that id")` / `errorReply("internal_error", "credential store is unavailable")` -- one home for the mapping the `Always` list requires all four call sites to share, so it cannot drift by copy-paste. Nothing else in this file changes (pool, cache key, invalidation, `closeAll` untouched).
- [x] `src/core/executor.ts` -- delete the private `targetError` (`:434-439`) and import the shared one; the two call sites (`:699`, `:707`) keep their exact text. Then wire DW-45: after a SUCCESSFUL `runQuery` on the confirmed-mutation branch of `executeRaw` (`:488-489`) and after `executeCreateTable` returns `status:"ok"`, call `seams.invalidateSchema()` before building the reply -- scoped to the resolved target only. Do NOT invalidate on the read branch, the `confirmation_required` branch, `insert`/`update`/`delete`, or any failed/throwing run. Document why the opaque confirmed-raw branch invalidates unconditionally rather than sniffing a DDL verb: this file is default-deny, and under-invalidating re-serves a stale schema to the AI (the DW-45 bug) while over-invalidating costs at most one extra `listSchema`.
- [x] `src/shared/contract.ts` -- add `connectionId?: string | null` to `TableRowsRequest` (`:344-349`) with the same doc wording `ExecuteRequest` uses (`:901-903`); add `ConnectRequest = { readonly connectionId?: string | null }` beside `ConnectResult` (`:324-330`) and `ChatStreamRequest = { provider: ProviderKind; message: string; connectionId?: string | null }` as the documented `/chat/stream` body; update the `ConnectionSummary.schema` doc (`:382-391`) that currently says the schema tree/table browser/chat "do not consult it yet ... is Story 10.4" -- it ships here, so the note must describe what is now true (Core resolves by id; the UI adopting it is 10.5/10.6). These are type-only declarations — the repo has no runtime validator library and each handler still narrows `unknown` by hand.
- [x] `src/core/rpc.ts` -- widen `RpcContext.connect` (`:40`) to `(params: unknown) => Promise<RpcReply<ConnectResult>>` with a doc comment matching `tableRows`'s (`:60-66`), and change the handler (`:149`) to `connect: async (params, ctx) => preformed(await ctx.connect(params))` -- byte-for-byte the `table.rows` handler shape (`:257`). The wire reply for a paramless `connect` is unchanged (`preformed(okReply(x))` serializes exactly as dispatch's auto-`okReply(x)`); validation stays in `server.ts`, exactly as it does for `table.rows`/`execute`.
- [x] `src/core/server.ts` -- thread the id through all three seams, resolving via the ALREADY-constructed `connectionTargets` (`:335-340`): (a) `tableRows(params)` validates `connectionId` is `string | null | undefined` BEFORE the try (mirroring `executor.ts:673-686`) → `errorReply("bad_request", "table.rows 'connectionId' must be a string or null")`, then inside the try resolves and returns `targetError(resolved.reason)` on failure, and runs `getSchema`/`quoteIdent`/`runQuery` against `resolved.seams` instead of `connectionManager` — the existing `NoConnectionTargetError` catch stays verbatim; (b) `rpcContext.connect` (`:445`) becomes `(params: unknown) => Promise<RpcReply<ConnectResult>>` doing the same validate → resolve → `okReply(await resolved.seams.connect())` (a classified failure rides INSIDE `okReply` as `{status:"failed"}`, never an envelope error); (c) the `createChatResponder` `getSchema` (`:358`) becomes `(connectionId) => { const r = connectionTargets.resolve(connectionId); if (!r.ok) throw new NoConnectionTargetError(); return r.seams.getSchema(); }`. `activeConnection` (`:448`) stays boot-only — out of scope. Move the `tableRows` definition below `connectionTargets` if declaration order requires it, changing nothing else about it.
- [x] `src/core/chat.ts` -- widen `ChatResponderDeps.getSchema` (`:190-199`) to `(connectionId?: string | null) => Promise<DatabaseSchema>` and update its doc (it is no longer "the boot manager's memoized getSchema"). In `prepareRequest` (`:237-303`), read `p.connectionId` right after the `message` check and reject a non-`string`/non-`null` value with the file's existing helper as `badRequest("invalid connectionId", "field=connectionId")` — same `bad_request` code, same `field=` detail convention as `provider`/`message`, no new error code — then pass it to `deps.getSchema(connectionId)` at `:277`. The `catch` at `:278-281` is untouched, so an unresolvable id lands on the existing "no active connection" reply. `assemblePayload`/`rowSample: null` untouched.
- [x] `src/core/connection.test.ts` -- add an `invalidateSchema` describe using this file's existing fake-driver harness: `getSchema()` twice returns the memo (one `listSchema` call); after `invalidateSchema()` the next `getSchema()` re-introspects and returns the NEW schema; `connect()` after `invalidateSchema()` likewise returns a `ConnectResult` carrying the fresh schema; two concurrent `getSchema()` calls after one `invalidateSchema()` trigger exactly ONE `listSchema` (single-flight); a `DriverConnectionError` thrown by the refresh makes `connect()` return neutral `{status:"failed"}` (message credential-free) while `getSchema()` rejects, and a later successful refresh still lands; `invalidateSchema()` on a never-connected and on a closed manager is a harmless no-op -- proves DW-45 is fixed at its root without a live DB.
- [x] `src/core/connection-targets.test.ts` -- extend the `fakeManager`/`harness` factories (`:21-74`) with the two new `ConnectionManager` members and assert both seams delegate to the RIGHT manager: `resolve(null).seams.connect()` hits the boot manager only; `resolve("conn-a").seams.connect()` hits that target's manager only; `resolve("conn-a").seams.invalidateSchema()` marks ONLY that manager (boot and `conn-b` untouched) -- locks the "scoped to the affected target" constraint.
- [x] `src/core/executor.test.ts` -- add the two new members to `makeSeams` (`:61-80`) and the two inline `ConnectionSeams` literals (`:1005`, `:1042`), recording `invalidateSchema` calls; then, in a new describe beside the targeted-routing block (`:914-1071`), assert the DW-45 matrix: confirmed raw mutation → invalidated once on the resolved target; auto-classified read → never; `confirmation_required` → never; `createTable` success → invalidated; `insert`/`update`/`delete` → never; a throwing `runQuery` → never. Also assert a targeted mutation invalidates the TARGET's seams and not the boot seams -- the scoping guarantee, provable with `makeTargetedExecutor`.
- [x] `src/core/server.test.ts` -- add targeted RPC coverage using the existing Core-boot harness with an injected `createDriver` and a connection saved via `connections.add`: `table.rows` and `connect` each with (i) a malformed `connectionId` (a number) → `bad_request` before any driver call, (ii) an unknown id → `not_found` "no connection with that id", (iii) a valid id → the reply reflects THAT target (its schema/rows, not the boot connection's), and (iv) assert no reply body or error detail contains the url/user/password substring. The existing untargeted tests (`:145-163`, `:376`, `:410`, `:452-453`) must stay byte-identical -- this is the AC's "byte-identical default path" evidence.
- [x] `src/core/rpc.test.ts` -- update `stubCtx`'s `connect` (`:112-164`) to the new `(params) => Promise<RpcReply<ConnectResult>>` contract (return `okReply(...)`) and add a dispatch assertion that `params` reach `ctx.connect` verbatim, mirroring the `table.rows` case. The `methodNames()` verbatim list (`:192-209`) and the paramless `connect` dispatch result (`:219-246`) must remain unchanged -- a mechanical stub update, not a behavior change.
- [x] `src/core/chat.test.ts` -- extend `makeResponder` (`:77-97`) to record the `connectionId` its `getSchema` receives, then add: a targeted `answerStream({provider, message, connectionId:"conn-b"})` forwards `"conn-b"` to `getSchema` and still emits `rowSample: null` in the outbound payload; a malformed `connectionId: 5` → `{type:"error", code:"bad_request"}` with a `field=connectionId` detail and ZERO outbound provider calls; a `getSchema` that throws for the id → the existing `"no active connection"` error chunk verbatim; and every existing paramless test (`:332-364`) stays green with `getSchema` receiving `undefined`/`null` -- covers the I/O matrix's chat rows.

**Acceptance Criteria:**
- Given the `connect` RPC dispatched with a `connectionId` for a saved connection whose driver fails to open, when it runs, then the reply is `okReply({status:"failed", failure, message})` — a domain outcome, never an error envelope and never an `internal_error`.
- Given `table.rows` with a valid `connectionId`, when the target's schema lacks the requested table, then the reply is the SAME `not_found`/`bad_request` `planTableRows` already produces for the boot path — target selection changes which schema is validated against, never the validation contract.
- Given any of `connect`, `table.rows`, or chat resolving an id, when the resolution fails, then no reply, error detail, or log line contains a URL, username, or password.
- Given `bunx tsc --noEmit`, when run, then it is clean — including the three `ConnectionSeams` literals in `executor.test.ts`, which must be updated rather than cast.

## Design Notes

**Why `connect` must become `preformed`.** A normal handler's return value is auto-wrapped by `dispatch` in `okReply` (`rpc.ts:319-323`), so it has no way to emit `bad_request`/`not_found`. Shape-checking `connectionId` and mapping a resolve failure both require emitting an error envelope, so `connect` joins `table.rows`/`execute` as a preformed handler. This is the only ctx-contract break in the story; the wire bytes for a paramless `connect` are identical, which is why `rpc.test.ts`'s `stubCtx` update is mechanical and NOT a behavior change. Same for `RpcContext.connect`'s type.

**Where `targetError` lives.** Three new call sites would otherwise copy `executor.ts:434-439`, and a fourth copy is exactly how the `not-found → not_found` / `unavailable → internal_error` convention drifts. `connection-targets.ts` owns the `reason` union, so it owns the mapping; `errorReply` returns `RpcReply<never>`, which is assignable into `RpcReply<TableRowsResult>` / `RpcReply<ConnectResult>` / `RpcReply<ExecuteResult>` positions without a generic parameter.

**Chat's malformed-id handling is a shape check, not a new error code.** The intent contract requires that a *resolve failure* reuse the existing "no active connection" catch and that no new chat error code appear. A non-string `connectionId` is a different thing — a protocol violation — and `prepareRequest` already rejects those for `provider` and `message` with `bad_request` + a `field=` detail. Reusing that exact code and convention keeps the promise (no new code) while matching both the file's own discipline and the executor's "validate the shape before any round-trip" rule.

**Why the chat binding throws `NoConnectionTargetError`.** `prepareRequest`'s catch is bare (`catch {}`), so any throw would work — but the typed error already means "there is no connection target for this read" everywhere else in the Core (`connection.ts:209`, `executor.ts:712`, `server.ts:322`), so a resolve miss raising it keeps one vocabulary. The cost is deliberate and contract-blessed: chat cannot distinguish `not-found` from `unavailable`. RPC callers can.

**DW-45: the memo must be bustable in place, not by eviction.** `connectionTargets` already invalidates a target by evicting + closing + recreating its manager — but that machinery cannot serve the boot manager, which `resolve(null)` returns and `server.ts` (not the pool) owns and closes. Since DW-45's fold-in explicitly covers "the boot default", the invalidation has to reach the memo itself, which lives in `connection.ts`'s `cached` closure with no reset path (`close()` is latched and permanent). Hence a flag + in-place re-introspect, exposed as one more 1:1 seam so the executor — which holds only the resolved seams — can scope the bust to exactly the target it just mutated, with no new plumbing and no way to flush the whole pool.

**Refresh failure is asymmetric on purpose.** `connect()` is documented as never throwing for a classified driver failure (`connection.ts:100-103`, `:135-162`), while the read path (`getSchema`) throws when the connection is unusable (`:195-216`). A refresh that fails must respect whichever contract it is running under — neutral `{status:"failed"}` out of `connect()`, a rejection out of `getSchema()` — and must leave `schemaStale` set so the next call retries rather than silently serving the stale memo forever.

**Not in this story:** `activeConnection` stays boot-only, and no UI file changes. `SchemaTree.tsx`, `TabContent.tsx`, and `rpc-stream.ts` keep sending no id, which is precisely why the default path staying byte-identical is the load-bearing constraint — the UI adopting the capability is 10.5 and 10.6.

## Verification

**Commands:**
- `bunx tsc --noEmit` -- expected: no diagnostics (in particular, the three `ConnectionSeams` literals in `executor.test.ts` compile with both new members present).
- `bun test` -- expected: full suite green; the ~1403-test baseline grows only by the new cases, with no existing assertion edited except `rpc.test.ts`'s `stubCtx.connect` and the seams/manager test factories. No test opens a database.
- `bun run build` -- expected: all four build scripts complete without error.

**Manual checks (if no CLI):**
- Read `server.ts`'s `tableRows`, `rpcContext.connect`, and the `createChatResponder` binding and confirm NONE of the three references `connectionManager` directly any more — each goes through `connectionTargets.resolve(...)`, with the boot manager reachable only as `resolve(null)`.
- Confirm `executor.ts` calls `seams.invalidateSchema()` on exactly two success paths (confirmed raw mutation, `createTable`) and nowhere else.

## Folded Deferred-Work

**DW-45 (fold-in): re-introspect after DDL — bust the memoized schema.** `src/core/connection.ts`'s `getSchema()` memoizes the schema at first connect and never re-introspects, so the tree/chat "N tables" context goes stale after a `create`/`drop table`. Because this story already routes reads through `connectionTargets.resolve(id)` — whose pool already invalidates a target's cached manager when the connection is repointed against the registry — extend that same invalidation to also fire after a successful schema-mutating `execute` (createTable / drop) on a target, so the NEXT `getSchema`/`connect` for that `connectionId` (or the boot default) re-introspects instead of serving the stale memo. Keep it SCOPED to the affected target — do not flush every target's cache. Mark DW-45 done in `deferred-work.md` when landed. (Ordering note: this pairs with Story 10.5's per-root refresh — the UI trigger — but the memo-bust itself is a Core concern and belongs here.)

Implementation shape decided during planning (see Design Notes): the bust is an in-place `invalidateSchema()` on `ConnectionManager`, surfaced as a `ConnectionSeams` member so the executor scopes it to the seams it just mutated. Eviction-based invalidation was rejected because it cannot reach the boot manager, which the fold-in explicitly covers.

## Spec Change Log

<!-- populated by later revisions -->

## Review Triage Log

### 2026-07-21 — Review pass
- intent_gap: 0
- bad_spec: 0
- patch: 5: (high 1, medium 4, low 0)
- defer: 6: (high 0, medium 2, low 4)
- reject: 5: (high 0, medium 0, low 5)
- addressed_findings:
  - `[high]` `[patch]` **DW-45 was defeated permanently and silently by a lost update.** `refreshIfStale` cleared `schemaStale` unconditionally when its `listSchema` settled, so an `invalidateSchema()` raised WHILE a refresh was in flight was swallowed by that refresh's older answer — the newer DDL became invisible forever, with nothing left to re-arm the flag. Reproduced by both reviewers. Added a monotonic `schemaGeneration` counter bumped by every `invalidateSchema()`; both commit sites (`refreshIfStale` and `open()`) capture it before awaiting `listSchema` and only write `cached` / clear the flag if it is unchanged. `refreshIfStale` became a loop so a joiner that invalidated after the in-flight refresh started re-tests the flag and runs its own introspection instead of accepting a provably-stale answer; single-flight is preserved for readers under one invalidation. Regression test verified failing before the fix.
  - `[medium]` `[patch]` **A refresh landing after `close()` resurrected the memo of a torn-down connection**, so `connect()` answered `{status:"connected"}` over a released driver instead of the documented neutral failure — and the same window was reachable mid-session via `connection-targets`' fire-and-forget `evict()`. The commit guard now also refuses to write once `closed` is latched, `close()` awaits any in-flight `refreshing` (swallowing its failure — shutdown must not be blocked), and `doConnect`'s `if (closed)` guard was hoisted ABOVE the idempotent `cached` branch to close the latch-before-null window. Regression test verified failing before the fix.
  - `[medium]` `[patch]` **The "over-invalidating costs at most one extra `listSchema`" claim was false by an order of magnitude.** `seamsFor.getEngine` was `(await manager.getSchema()).engine`, so every raw `execute` — which opens with `await getEngine()` — paid a full re-introspection after the previous statement busted the memo: five confirmed `UPDATE`s measured five `listSchema` calls (five introspection queries each on Postgres), serialized before each statement. Added `ConnectionManager.getEngine()`, which reads the engine off the connection's own memo WITHOUT honoring the stale flag (the engine is a property of the connection, not of the catalog), and made the seam a 1:1 delegation like every other. Measured 5 → 1. The false cost comment in `executor.ts` was rewritten honestly.
  - `[medium]` `[patch]` **A non-object `params` silently fell back to the boot connection with an `ok:true` reply**, so `{"method":"connect","params":"conn-b"}` returned the BOOT database's schema with no signal that targeting was ignored — for a story whose entire subject is "which database am I reading". This contradicted the very block it was documented to mirror (`executor.ts` rejects a non-object request). `readConnectionId` now rejects a `params` that is present and not a plain object; `undefined`/`null` still means "no id", so the UI's paramless `connect` stays byte-identical. Rejecting misspelled/unknown FIELDS remains out of scope.
  - `[medium]` `[patch]` **Nothing tested DW-45 end to end** — all fifteen assertions stopped at a fake boundary (fake seams / fake driver / fake manager), never composing `execute(DDL) → invalidateSchema → next getSchema re-introspects`, which is precisely why the two proven defects above were invisible to a green suite. Extended `server.test.ts`'s `withTargetedCore` harness with mutable per-url catalogs and an introspection counter, and added a real-Core test: a table created through the `execute` RPC appears in the next `connect` for that target (introspections 1→2) while the boot catalog serves its memo untouched.

### 2026-07-21 — Review pass (follow-up)

- intent_gap: 0
- bad_spec: 0
- patch: 9: (high 2, medium 2, low 5)
- defer: 2: (high 0, medium 1, low 1)
- reject: 8: (high 0, medium 5, low 3)
- addressed_findings:
  - `[high]` `[patch]` **`connect()` could resolve `null`, and the compiler could not see it.** In `doConnect`'s idempotent branch, TypeScript carries the narrowing from `cached !== null` across the `await refreshIfStale(driver)` — but `close()` latches `closed`, awaits that very refresh, and only THEN nulls `cached`, so a `connect` parked on it resumes into a torn-down manager and returns the nulled memo. `server.ts` wraps whatever it gets (`okReply(await seams.connect())`), shipping `result: null` to a UI that reads `reply.result.status`. The window is not exotic: `connection-targets`' fire-and-forget `evict()` opens it mid-session on any Settings repoint. Reproduced independently by the Edge Case Hunter. Fixed by re-reading `cached` into a local and answering the same neutral `{status:"failed", failure:"network"}` shutdown payload the `closed` guard above already returns. Regression test verified failing before the fix (returned `null`).
  - `[high]` `[patch]` **The refresh loop had no ceiling, so a writer could hang a reader outright.** `refreshIfStale`'s `while (schemaStale && !closed)` only terminates while introspection out-runs invalidation; sustained schema-mutating traffic on a target (every `execute` success invalidates) inverts that, and a single `getSchema()`/`connect()` then never settles. Reproduced by the Blind Hunter (one reader, 200+ introspections and still going) and by the Edge Case Hunter. Bounded to `MAX_REFRESH_TURNS = 3` turns, counting joins so a caller cannot park in the join branch either; on giving up the stale flag stays SET, so the caller serves a memo at most a few DDLs behind and the next reader resumes the chase — bounded staleness traded for bounded latency, which is the cheaper failure. Regression test verified against the pre-fix code: it did not merely fail, it starved the event loop and hung the test process until killed.
  - `[medium]` `[patch]` **The chat targeting wiring had zero coverage.** `server.ts`'s `getSchema: (connectionId) => resolve-or-throw` lambda — the third of this story's three seams — was asserted nowhere: `chat.test.ts` stops at a fake `getSchema`, and every Story 10.4 server test entered through `/rpc`. Added an end-to-end test driving the real `/chat/stream` with a configured provider key and an unresolvable id, asserting the stream is exactly one `bad_request` "no active connection" chunk, that the id does not ride back in the bytes, and that NO driver was opened (chat does not silently fall back to the boot connection when an id was sent). The resolvable half stays uncovered end to end — `generateStream` is not injectable through `StartCoreOptions`, and widening a production type to make a test possible was refused.
  - `[medium]` `[patch]` **The contract's new pinned-schema claim had no evidence.** `ConnectionSummary.schema` now states that a request carrying a `connectionId` honors that connection's Story 10.2 pin — the claim that decides which catalog a targeted read is validated against — while `server.test.ts`'s fake driver declared `async listSchema()` with no parameters and discarded the pin entirely. Made the fake honor it and added a test on its own url: a `connect` for the pinned connection sees only the pinned scope, a `table.rows` for an out-of-scope table is `not_found` (the pin is load-bearing, not cosmetic), and the unpinned target and boot calls are unmoved.
  - `[low]` `[patch]` `getEngine()` awaited `ensureDriver()` twice, discarding the first driver handle.
  - `[low]` `[patch]` Three doc sites claimed `getEngine` is "answered from the CONNECTION (its url scheme), never by re-introspecting the catalog". It reads `cached.schema.engine`, so a COLD manager does introspect once (as any seam on it would) — the diff's own test shows that call. Reworded to the true claim: populated by the first open, never re-introspected, deliberately ignoring the stale flag.
  - `[low]` `[patch]` `ConnectionManager.connect`'s JSDoc still said only "Idempotent" after this story hoisted the `closed` check above the idempotent branch — an observable contract change (post-close it now answers the neutral failure instead of the memo) recorded nowhere. Documented at the type.
  - `[low]` `[patch]` `close()`'s comment claimed awaiting `refreshing` "guarantees the refresh is not still reading the driver". It drains only the flight visible at that instant; what makes it total is the `closed` latch in the refresh loop. Rewritten to name the real invariant, so loosening that loop condition cannot silently invalidate the comment.
  - `[low]` `[patch]` `executor.test.ts`'s `if (opts.queryThrows)` was a truthiness check on an `unknown`, so a falsy sentinel would silently disable the throwing seam and turn failed-mutation cases into passing ones.

## Auto Run Result

Status: done

**Implemented change.** The Core no longer reads through a single boot connection. `connect`, `table.rows` and chat now resolve their live manager through the SAME `connectionTargets.resolve(connectionId)` pool the Reports executor has used since Story 6.2 — lazy, cached by opaque id, self-invalidating against the registry. `connectionId` is optional everywhere: absent or `null` resolves the boot manager, so Ephemeral mode and every existing call site are byte-identical (the UI still sends no id — adopting it is 10.5/10.6). `ConnectionSeams` gained three 1:1 delegations (`connect`, `invalidateSchema`, `getEngine`), `connect` became a `preformed` RPC handler so it can shape-check its id and emit a typed envelope, and the `not-found → not_found` / `unavailable → internal_error` mapping moved into ONE exported `targetError` beside the union it translates, replacing the executor's private copy. Only the opaque id ever crosses the loopback (AR-12); the chat payload stays schema-only (`rowSample: null`).

**DW-45 (folded) is resolved.** `ConnectionManager` gained `invalidateSchema()`, which busts the memoized schema IN PLACE — a stale flag plus a generation-guarded, single-flight re-introspection under the same pinned scope — surfaced as a seam so the executor scopes the bust to exactly the target it just mutated (successful confirmed raw mutation + successful `createTable`; row DML never invalidates). Eviction-based invalidation was rejected: it cannot reach the boot manager, which `resolve(null)` returns and `server.ts` owns for the whole session.

**Files changed.**
- `src/core/connection.ts` — `invalidateSchema()` + `getEngine()` on `ConnectionManager`; `schemaStale` / `schemaGeneration` / single-flight `refreshIfStale` with a commit guard and a `MAX_REFRESH_TURNS` ceiling; `doConnect` honors a pending invalidation, its `closed` check was hoisted above the idempotent branch, and its post-refresh re-read of the memo answers the neutral shutdown failure when `close()` nulled it underneath; `close()` awaits any in-flight refresh.
- `src/core/connection-targets.ts` — `ConnectionSeams` gains `connect`/`invalidateSchema`, `getEngine` becomes a 1:1 delegation; new exported `targetError`. Pool, cache key, eviction and `closeAll` untouched.
- `src/core/executor.ts` — uses the shared `targetError`; invalidates the resolved target's memo after a successful schema-mutating run.
- `src/core/server.ts` — `connectionTargets` hoisted above the read capabilities; `readConnectionId` shape-check helper; `tableRows` and the new `connect(params)` capability resolve and run against the resolved seams; chat's `getSchema` resolves per request.
- `src/core/rpc.ts` — `RpcContext.connect` widened to the `(params) => RpcReply<ConnectResult>` shape; handler is now `preformed`, wire bytes for a paramless `connect` unchanged.
- `src/core/chat.ts` — `getSchema` accepts an optional `connectionId`; a malformed one is a `bad_request` with the file's existing `field=` convention; an unresolvable one reuses the existing "no active connection" catch.
- `src/shared/contract.ts` — `TableRowsRequest.connectionId`, new `ConnectRequest` / `ChatStreamRequest`, and the now-false `ConnectionSummary.schema` note about 10.4 being pending rewritten.
- Tests — `connection.test.ts`, `connection-targets.test.ts`, `executor.test.ts`, `rpc.test.ts`, `chat.test.ts`, `server.test.ts` (+42 tests, no existing assertion changed; only test factories/harnesses were widened — including the `withTargetedCore` fake driver, which now honors the pinned introspection scope).
- `_bmad-output/implementation-artifacts/deferred-work.md` — DW-45 marked done; eight new deferred entries across the two review passes.

**Review findings breakdown (two passes).** Pass 1: 5 patches (1 high, 4 medium), 6 deferred, 5 rejected. Pass 2 (this follow-up): 9 patches (2 high, 2 medium, 5 low), 2 deferred, 8 rejected. Across both: 0 intent gaps, 0 spec defects, 0 loopbacks.

Pass 1's high-severity patch: an `invalidateSchema()` raised while a refresh was in flight was swallowed by that refresh's older answer, making the newest DDL invisible forever — DW-45 returning permanently and silently behind a green suite. Fixed with a generation counter checked at both commit sites. Also patched there: a refresh landing after `close()` resurrecting the memo of a torn-down connection; a measured 5x re-introspection cost because `getEngine` routed through the memoized `getSchema`; a non-object `params` silently falling back to the boot connection with an `ok:true` reply; and the absence of any end-to-end DW-45 test.

Pass 2 found two more HIGH defects — both in the concurrency machinery pass 1 itself introduced, both reproduced by execution, neither visible to a green suite. (a) `connect()` could resolve **`null`**: TypeScript keeps the `cached !== null` narrowing across the refresh await while `close()`/`evict()` nulls the memo underneath it, so the RPC layer would ship `result: null` to a UI that reads `reply.result.status`. (b) The refresh loop had **no ceiling**, so sustained schema-mutating traffic on a target starves any concurrent read: the regression test does not merely fail against the pre-fix code, it hangs the test process. Both fixed with regression tests verified against the pre-fix code. Pass 2 also closed the two remaining coverage gaps (the chat targeting lambda, the contract's pinned-schema claim) and five doc/dead-code inaccuracies.

**Follow-up review recommended: true.** This pass changed production concurrency semantics again — a new bounded-staleness policy (`MAX_REFRESH_TURNS`) and a shutdown-race answer for `connect()` — and both prior passes proved that this specific machinery is where defects hide from a green suite. The signal is converging (no new API, no wire change, and the fixes are a guard and a loop bound rather than new mechanism), but the change is behavioral and landed after the reviewed diff, so it has not been adversarially reviewed itself.

**Verification performed.** `bunx tsc --noEmit` clean; `bun test` **1445 pass / 0 fail** across 73 files (1441 after pass 1, 1434 at the end of implementation, 1403 before the story); `bun run build` exit 0, all four bundles written. No test opens a real database. Every regression test for a proven defect was confirmed failing (or hanging) against the pre-fix code, restoring the fix from a verified copy afterwards. The delegated test-only patches were re-read diff-by-diff before acceptance.

**Residual risks.** The targeted path is exercised only by tests — the UI still sends no `connectionId`, so real usage covers just the default path. The resolvable half of the chat targeting path has no end-to-end test (`generateStream` is not injectable through `StartCoreOptions`, and widening a production type for a test was refused). Under sustained DDL on one target a reader now accepts a memo a few DDLs behind rather than chasing forever — bounded staleness is deliberate, and the stale flag stays armed so the next reader converges. A transient introspection failure after a mutation still turns a read that previously served the memo into a hard error (deferred: no last-known-good fallback). Two ids addressing the same database keep independent memos (deferred: pool identity is keyed by id, by contract). A `runQuery` that throws after partially applying DDL leaves the memo stale (deferred: the spec mandates success-only invalidation). A connection removed mid-flight still surfaces as `internal_error` rather than a typed `not_found`, and during shutdown a targeted `connect` answers a misleading 404 (both deferred). The target pool still has no bound or idle eviction — inert today, reachable once 10.5/10.6 make the UI send ids (deferred).
