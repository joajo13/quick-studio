---
scope: quick-studio v1 whole-product architecture — lightweight local-first DB manager (Postgres+MySQL), one-command install/run, local web UI on 127.0.0.1, multi-provider AI chat with executable-JS MDX blocks, local report generation. Platforms: Windows + Linux.
purpose: Build substrate spine (terse, convergent) for greenfield build + a portfolio-grade HTML solution-design doc as human-facing rendering.
altitude: Initiative/product (above epics) — spine fixes v1-wide invariants that features/epics must share.
updated: 2026-07-06T02:05
---

- (decision) Working mode = Coaching path (user chose). Load-bearing calls shown not silently drafted.
- (decision) Deliverables = spine (build substrate) + portfolio-grade HTML solution-design doc at finalize. Rationale: PRD is portfolio-grade, open-source door open.
- (direction) Input = final PRD + brief + addendum (greenfield, no code). Architecture must resolve first: §11-R1 executable-JS sandbox (gates FR-17), Open Q1 charting fit, Open Q3 Live Report data path. Plus greenfield stack/starter, credential store model, per-platform packaging.
- (decision) [ADOPTED] Stack = full TypeScript (TS server + React UI, single language end-to-end). Binds: all. Prevents: two-language split / ecosystem impedance for the JS-heavy AI/MDX/chart core. Rationale (user): TS fluency, 150-200MB idle footprint accepted (defuses the lightweight->Go/Rust pressure), portfolio stack indifferent. Runtime (Bun vs Node) + packaging = open sub-fork pending research.
- (question) OPEN sub-fork: TS runtime = Bun (bun build --compile single-file) vs Node (SEA / pkg). Decide on research: single-binary maturity, idle footprint, cold-start vs the 2s budget, Win+Linux cross-compile.
- (version) Verified 2026: Bun 1.2.x (bun build --compile, ~35-45MB idle); Vercel AI SDK 'ai' 7.0.15 + @ai-sdk/{anthropic,openai,google} 4.0.8 (unified streaming+reasoning); @anthropic-ai/sdk 0.110.0; openai 6.45.0; @google/genai 2.10.0 (NOTE @google/generative-ai DEPRECATED); Recharts 3.9.2 (=shadcn charts, still Recharts); Observable Plot 0.6.17 (framework-agnostic, for sandbox); pg8/postgres.js + mysql2 3.22.5; @napi-rs/keyring 1.3.0 (keytar DEAD/archived); TradingView lightweight-charts=WRONG (OHLC).
- (decision) [ADOPTED] Charting split by trust ring (resolves Open Q1): Recharts/shadcn in-app (main origin), Observable Plot in sandbox (no React host there, agnostic lib). Prevents: assuming one chart lib works in both rings. TradingView dropped.
- (decision) Sandbox mechanism refined: cross-origin iframe is the ONLY real security boundary (in-process SES/QuickJS/ShadowRealm = integrity only, vulnerable to Spectre-class reads of credential heap). Rule: sandbox='allow-scripts' WITHOUT allow-same-origin; CSP default-src 'none', connect-src 'none'; postMessage RPC only. Matches Claude Artifacts / ChatGPT canvas 2026 practice.
- (decision) AI layer = Vercel AI SDK 7 unified across Anthropic/OpenAI/Google; per-request user API keys; SSE streaming + normalized reasoning. Binds FR-14/15/16.
- (direction) Runtime sub-fork LEANING Bun (bun build --compile) over Node/Deno given full-TS + accepted 150-200MB. FLAG: @napi-rs/keyring NAPI parity on Bun 'almost not 100%' -> smoke-test Win+Linux before locking. Not yet adopted.
- (decision) [ADOPTED] Live Report data path (resolves Open Q3) = A1: exported Live Report re-queries only via a running quick-studio on 127.0.0.1; no embedded runtime, no loose credentials in the HTML file. Binds FR-19/FR-20. Prevents: reopening the credential/query surface the three-ring model closed. Snapshot = frozen data in HTML, sandbox pure-render. Cost accepted: 'Live' portability requires quick-studio running.
- (decision) [ADOPTED] Root paradigm = three-ring trust model. Ring 1 Trusted Core (backend TS process): sole holder of DB connections, decrypted credentials, Provider API keys; sole executor of SQL; home of destructive-statement guard + port-exposure watcher. Ring 2 Semi-trusted UI (main browser origin, React/shadcn): orchestrates, no direct DB/credential access, asks the core over 127.0.0.1 HTTP/SSE. Ring 3 Untrusted Sandbox (cross-origin iframe): runs LLM-generated MDX JS; pure-render only; no network/fs/backend/credential/SQL. Data flows outward only. Binds: all. Prevents: any feature building a path that lets less-trusted code reach DB/credentials.
- (decision) [ADOPTED] AI->Provider privacy policy (§11-R5): DEFAULT schema-only (table/column names, types, FKs; never row data). Sample rows are opt-in, explicit, PER-QUERY, and visibly indicated in the UI when active. Binds FR-15/FR-17, §11-R5. Prevents: silent exfiltration of real data to a Provider; two chat surfaces disagreeing on what leaves the machine. Only outbound channel is the user-configured Provider; nothing else leaves.
- (decision) [ADOPTED] Distribution (resolves Open Q4) = P3: publish BOTH (a) npm/bun global package 'quick-studio' for devs with a runtime, and (b) standalone bun build --compile binaries per platform via GitHub Releases + one-liner install for those without. Binds FR-1/FR-2. Primary/headline path = standalone binary (matches 'one command, disposable, no installer'). Prevents: Linux colleagues without Bun being unable to install.
- (decision) [ADOPTED] Runtime = Bun (bun build --compile for standalone binaries; npm-compatible ecosystem for pg/mysql2/@napi-rs/keyring/AI SDK/React tooling). Idle ~35-45MB within accepted 150-200MB. Cold start well under 2s budget.
- (assumption) [ASSUMPTION] @napi-rs/keyring NAPI parity on Bun 'almost not 100%' -> requires smoke-test on Windows + Linux before locking; passphrase fallback (FR-5) is the safety net if a platform fails.
- (decision) [ADOPTED] Core<->UI transport = local HTTP + SSE over 127.0.0.1 for streaming (AI tokens/reasoning, query results). Credential Store on disk: AES-256-GCM file at OS-convention path (%APPDATA% / XDG_DATA_HOME); key in OS keychain. Ephemeral mode = zero disk writes (hard invariant).
- (event) Reviewer Gate dispatched: lint_spine PASS (0 findings). Parallel subagents: reconcile-vs-PRD, rubric walker, adversarial divergence, tech-currency. Tech-currency subagent glitched (0 tool uses) -> currency lens covered by the initial verified research (all Stack versions sourced from npm registry / release pages).
- (event) Reviewer Gate results: reconcile=GAPS(low), rubric=NEEDS WORK, adversarial=HOLES FOUND (2 critical). Applied all: TIGHTENED AD-3 (one-way sandbox data channel, closes query-laundering H2), AD-4 (Core sole classifier + UPDATE/multi-statement default-deny H3), AD-7 (Core-owned typed Provider payload, schema/rows separated H4). NEW AD-12 (Core authenticates caller: session token + Origin/Host anti-rebinding, closes critical H1 DNS-rebinding), AD-13 (canonical frozen-data shape + wire rules on UI<->Sandbox H5), AD-14 (engine-dialect isolation + Core-owned pagination, dialect finding + H6), AD-15 (single OS-convention persistent dir, rubric HIGH on-disk locations), AD-16 (dual distribution P3, fixes Deferred mislabel). Added Performance Budgets section homing NFR-1..6 as acceptance criteria. Conventions +dark-first(§12) +R6 no-account +wire rules extended to sandbox. Fixed Stack freshness wording.
- (event) Rendering produced: solution-design.html (portfolio-grade, dark-first shadcn aesthetic, self-contained). No external_handoffs configured. Spine set status:final.
- (event) spine finalized
