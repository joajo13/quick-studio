{
  "name": "quick-studio",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Lightweight local-first DB manager (Postgres + MySQL) — walking skeleton",
  "scripts": {
    "dev": "bun run bin/quick-studio.ts",
    "build": "bun build ./src/ui/main.tsx --outdir ./dist --target browser",
    "test": "bun test"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "typescript": "^5.7.0"
  }
}
